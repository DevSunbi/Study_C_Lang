# Week 12-13 시험 대비 정리

범위: `MD/Week12/Day50` ~ `MD/Week13/Day58`

원본 파일:

- `MD/Week12/Day50/Day50.md`
- `MD/Week12/Day51/Day51.md`
- `MD/Week12/Day51/BCM2711.md`
- `MD/Week12/Day52/Day52.md`
- `MD/Week12/Day53/Day53.md`
- `MD/Week13/Day54/Day54.md`
- `MD/Week13/Day55/Day55.md`
- `MD/Week13/Day56/Day56.md`
- `MD/Week13/Day58/Day58.md`

정리 기준:

- 원문 내용을 빠짐없이 시험용으로 재배치했다.
- 중복되는 내용은 한 곳에 통합했다.
- 오탈자, 오래된 API, 개념상 혼동될 수 있는 부분은 `[수정]`, `[주의]`, `[보충]`으로 표시했다.
- 원문 이미지의 내용은 문맥상 핵심 의미를 텍스트로 풀었고, 마지막에 이미지 참조 목록을 남겼다.

---

## 0. 먼저 외울 핵심 흐름

### Week 12 핵심 흐름

```text
사용자 프로그램
  -> open/read/write/ioctl 같은 시스템 호출
  -> VFS
  -> file_operations
  -> 캐릭터 디바이스 드라이버 함수
  -> 커널 API 또는 MMIO 레지스터 접근
  -> 실제 하드웨어 제어
```

```text
커널 모듈 작성
  -> Makefile로 .ko 빌드
  -> insmod/modprobe로 로드
  -> module_init() 실행
  -> /dev 장치 파일 생성 또는 자동 생성
  -> 사용자 앱이 장치 파일 접근
  -> module_exit()으로 정리 후 rmmod/modprobe -r
```

### Week 13 핵심 흐름

```text
전원 인가
  -> SoC ROM code
  -> 선택된 부트 장치에서 초기 부트로더 적재
  -> SPL/TPL/U-Boot proper 단계
  -> DRAM 초기화
  -> kernel image + DTB + initramfs/rootfs 정보 적재
  -> bootargs 전달
  -> Linux kernel entry point로 jump
  -> 커널 초기화
  -> rootfs mount
  -> /init 또는 systemd/init 실행
```

임베디드 리눅스 구성 요소:

```text
툴체인 + 부트로더 + 커널 + 루트 파일시스템 + 애플리케이션
```

Yocto가 하는 일:

```text
레시피(.bb)와 레이어 메타데이터를 BitBake가 실행
  -> 툴체인, 부트로더, 커널, 루트 파일시스템, 패키지, 이미지 생성
```

---

## 1. 정정 및 보충 사항

| 원문/혼동 지점 | 정정 및 보충 |
|---|---|
| `임눅스` | 리눅스 |
| 커널 소스 디렉터리 `fd`, `driver` | 일반적으로 `fs`, `drivers`가 맞다. |
| `static int_init hello_init` | `static int __init hello_init(void)`가 맞다. |
| `module_init(hello_exit)` | 종료 함수는 `module_exit(hello_exit)`로 등록한다. |
| `linux/malloc.h` | 최신 커널에서는 `kmalloc/kfree`에 보통 `<linux/slab.h>`를 사용한다. |
| `asm/uaccess.h` | 최신 코드에서는 보통 `<linux/uaccess.h>`를 사용한다. |
| `cli()`, `sti()` | 오래된/아키텍처 종속 API다. 최신 커널에서는 `local_irq_disable/enable`, `spin_lock_irqsave` 등을 문맥에 맞게 사용한다. |
| 64비트 페이지 크기 8KB | 페이지 크기는 아키텍처와 커널 설정에 따라 다르다. x86_64/ARM64에서 흔히 4KB이며 ARM64는 16KB/64KB 설정도 가능하다. |
| `register_chrdev(unsigned int majot, ...)` | `major` 오타. 구형 API이며 최신 드라이버는 `alloc_chrdev_region` + `cdev_init` + `cdev_add` 흐름을 선호한다. |
| `class_name("my_class")` | `class_create("my_class")` 또는 커널 버전에 따라 `class_create(THIS_MODULE, "my_class")` 형식이다. |
| `class_destory` | `class_destroy`가 맞다. |
| `device_create(struct class, ...)` | 첫 인자는 `struct class *class` 포인터다. |
| `.ioctl` | 최신 `file_operations`에서는 보통 `.unlocked_ioctl`을 사용한다. |
| `unnlocked_ioctl` | `unlocked_ioctl` 오타. 시그니처는 일반적으로 `long (*unlocked_ioctl)(struct file *, unsigned int, unsigned long)`이다. |
| `verify_area()` | 오래된 API다. 최신 코드에서는 `access_ok()`, `copy_to_user()`, `copy_from_user()` 반환값 확인을 사용한다. |
| `_IOC_NR(cmd) != SK_MAXNR` | 보통 `if (_IOC_NR(cmd) > SK_MAXNR)`처럼 범위 검사한다. `!=`로 검사하면 허용 번호 대부분이 거부된다. |
| `set_irq_type`, `IRQT_*` | 오래된 표기다. 최신 커널에서는 `irq_set_irq_type()`과 `IRQ_TYPE_EDGE_RISING/FALLING/BOTH`, `IRQ_TYPE_LEVEL_HIGH/LOW` 계열을 사용한다. |
| `include/uapi/asm-gernic/unistd.h` | `include/uapi/asm-generic/unistd.h`가 맞다. |
| 부트 순서에서 `SPL -> TPL` | U-Boot 일반 용어로는 Boot ROM -> optional TPL -> SPL -> U-Boot proper -> kernel 흐름이 더 일반적이다. 일부 보드 설명에서는 단계를 단순화하거나 다르게 부를 수 있다. |
| `obj-y` 설명 `무보건`, `퍼함` | `obj-y`는 조건 없이 커널 이미지에 내장 빌드한다는 뜻이다. |
| `pbj-$(CONFIG_TTY_PRINTK)` | `obj-$(CONFIG_TTY_PRINTK)`가 맞다. `y`면 내장, `m`이면 모듈, `n`이면 빌드하지 않는다. |
| QEMU `x84/x86_64` | `x86/x86_64`가 맞다. |
| Device Tree `interupt` | `interrupt`가 맞다. |
| Device Tree 경로 구분자 | 장치 트리 경로는 `/`를 사용한다. |
| Yocto 레이어 최소 파일 | 레이어에는 보통 `conf/layer.conf`, README, 라이선스 파일이 중요하다. `conf/local.conf`는 빌드 디렉터리 설정 파일이다. |
| `.bbapend` | `.bbappend`가 맞다. |
| `SUMMERY` | `SUMMARY`가 맞다. |
| `IMAGE_INSTALL_append` | Yocto scarthgap(5.0) 이후에서는 `IMAGE_INSTALL:append`처럼 콜론 문법을 사용한다. 구버전에서는 `_append`가 유효하다. |

---

## 2. 리눅스 커널과 디바이스 드라이버

### 2.1 커널 역할

커널은 운영체제의 핵심이며 다음 역할을 한다.

- CPU, 메모리, 저장장치, 네트워크, 입출력 장치 같은 시스템 자원 관리
- 하드웨어와 소프트웨어 사이의 인터페이스 제공
- 사용자 공간 프로그램이 직접 하드웨어를 만지지 않아도 되도록 추상화된 API 제공
- 시스템 호출, 인터럽트 처리, 스케줄링, 메모리 관리, 파일 시스템, 네트워크 스택, 디바이스 드라이버 관리

사용자 프로그램은 직접 하드웨어를 제어하지 않고 보통 다음 경로를 거친다.

```text
Application
  -> C Library
  -> System Call
  -> Kernel
  -> Device Driver
  -> Hardware
```

인터럽트 관점:

```text
Hardware event
  -> interrupt
  -> kernel interrupt handler
  -> driver processing
  -> user process wakeup/signal/read result
```

### 2.2 커널 선택 시 고려 사항

벤더 지원:

- SoC 또는 보드 벤더가 리눅스를 지원하는지 확인한다.
- BSP(Board Support Package), 커널 브랜치, 드라이버, 장치 트리, 부트로더 지원 수준을 확인한다.
- 메인라인 커널 지원 여부가 좋을수록 장기 유지보수가 쉽다.

라이선스:

- 리눅스 커널은 GPL v2다.
- 커널 자체 또는 커널에 링크되는 파생물은 GPL 영향을 받을 수 있다.
- 사용자 공간에서 시스템 호출만 사용하는 프로그램은 일반적으로 커널 GPL에 직접 종속되지 않는다.
- 커널 모듈 라이선스는 실무에서 논쟁과 예외가 있으므로 제품화 시 반드시 확인해야 한다.

커널 구조:

- 모놀리식 커널: 커널 기능이 하나의 주소 공간에서 동작한다. 성능이 좋지만 커널 내부 결합도가 높다.
- 마이크로커널: 최소 기능만 커널에 두고 나머지를 사용자 공간 서버로 분리한다. 모듈성과 안정성은 좋지만 IPC 비용이 생긴다.
- 하이브리드 커널: 두 구조를 절충한다.

리눅스는 모놀리식 커널이지만 Loadable Kernel Module(LKM)을 지원한다. 따라서 디바이스 드라이버, 파일 시스템, 네트워크 기능 등을 실행 중에 `.ko` 커널 오브젝트로 로드/언로드할 수 있다.

### 2.3 커널 모듈 위치와 확인 명령

현재 커널 버전 확인:

```bash
uname -r
```

커널 모듈 디렉터리:

```bash
/lib/modules/$(uname -r)/
```

자주 보는 하위 항목:

| 항목 | 의미 |
|---|---|
| `kernel/` | 실제 커널 모듈들이 기능별로 배치되는 디렉터리 |
| `build` | 현재 커널 모듈 빌드에 필요한 커널 빌드 디렉터리 링크 |
| `source` | 커널 소스 링크 |
| `modules.dep` | 모듈 의존성 정보 |
| `modules.alias` | 모듈 alias 정보 |
| `modules.order` | 빌드된 모듈 순서 |
| `modules.builtin` | 커널에 내장된 모듈 목록 |

예:

```bash
cd /lib/modules/$(uname -r)
ls
cd kernel
ls
```

`.ko` 파일은 Kernel Object 파일이며, 커널에 로드되어야 커널 공간에서 실행된다.

---

## 3. 커널 공간과 사용자 공간

### 3.1 사용자 공간

사용자 공간은 일반 애플리케이션과 명령어가 실행되는 영역이다.

예:

- `ls`
- `ps`
- `mount`
- 직접 컴파일한 사용자 프로그램

특징:

- 제한된 권한으로 실행된다.
- 하드웨어 레지스터나 커널 내부 메모리에 직접 접근할 수 없다.
- 시스템 호출을 통해 커널 기능을 요청한다.

### 3.2 커널 공간

커널 공간은 커널과 디바이스 드라이버가 실행되는 영역이다.

특징:

- 특권 모드로 실행된다.
- 하드웨어 제어, 인터럽트 처리, 스케줄링, 메모리 관리, 파일 시스템 처리 수행
- 잘못된 포인터 접근이나 동기화 오류가 시스템 전체 장애로 이어질 수 있다.

### 3.3 주소 공간

커널은 가상 주소를 사용한다. 실제 하드웨어 레지스터에 접근하려면 물리 주소 또는 I/O 물리 주소를 커널 가상 주소로 매핑해야 한다.

주소 종류:

| 주소 | 의미 |
|---|---|
| User Virtual Address | 사용자 프로그램이 보는 가상 주소 |
| Kernel Virtual Address | 커널이 사용하는 가상 주소 |
| Physical Address | CPU/MMU 관점의 물리 주소 |
| Bus Address | DMA 또는 주변장치 버스 관점의 주소 |
| MMIO Address | 메모리처럼 매핑된 장치 레지스터 주소 |

실제 하드웨어 레지스터 접근:

```text
Physical/MMIO address
  -> ioremap()
  -> kernel virtual address
  -> readl()/writel() 또는 포인터 접근
```

사용자 공간에서 `/dev/mem`을 사용할 때:

```text
/dev/mem open
  -> mmap()
  -> process virtual address
  -> physical address range 접근
```

커널 드라이버에서는 `/dev/mem` 대신 `ioremap()` 계열을 사용한다.

---

## 4. 커널 소스 디렉터리와 주요 하위 시스템

| 디렉터리 | 내용 |
|---|---|
| `kernel/` | 프로세스 생성/소멸, 스케줄링, 시그널, 커널 핵심 기능 |
| `arch/` | CPU 아키텍처별 코드. ARM, ARM64, x86 등 하드웨어 종속 코드 |
| `arch/arm/boot` 또는 `arch/arm64/boot` | 부트 이미지 생성 관련 코드 |
| `arch/arm/kernel`, `arch/arm64/kernel` | 문맥 교환, 트랩, 아키텍처별 커널 처리 |
| `arch/arm/mm`, `arch/arm64/mm` | 페이지 테이블, MMU, 페이지 폴트 등 메모리 관리의 아키텍처 종속 부분 |
| `fs/` | VFS, 파일 시스템, `open/read/write` 같은 파일 관련 시스템 호출 |
| `mm/` | 가상 메모리, 페이지, 커널 메모리 할당자 |
| `drivers/` | 디바이스 드라이버. 문자, 블록, 네트워크, 사운드, 비디오 등 |
| `net/` | TCP/IP, UNIX domain socket 등 네트워크 프로토콜 |
| `ipc/` | 파이프, 시그널, 메시지 큐, 공유 메모리, 세마포어 등 프로세스 간 통신 |
| `init/` | 커널 초기화. `init/main.c`의 `start_kernel()` 등 |
| `lib/` | 커널 내부에서 사용하는 공통 라이브러리 함수 |
| `include/` | 커널 헤더 |

커널 시작 흐름에서 중요한 함수:

```text
start_kernel()
  -> 각종 커널 자료구조 초기화
  -> 메모리/스케줄러/IRQ 초기화
  -> 드라이버 초기화
  -> rest_init()
  -> kernel_init()
  -> PID 1 사용자 공간 init 실행
```

---

## 5. 리눅스 커널 개요

### 5.1 프로세스와 스레드

프로세스:

- 실행 중인 프로그램
- 독립적인 가상 메모리 공간을 가진다.
- 커널 내부에서는 태스크로 관리된다.

스레드:

- 프로세스 내부의 실행 흐름
- 스케줄링의 최소 단위
- 같은 프로세스의 여러 스레드는 주소 공간을 공유한다.

커널의 태스크 구조체:

- 프로세스/스레드는 커널에서 `task_struct`로 표현된다.
- 상태, 스케줄링 정보, 메모리 정보, 파일 정보, 시그널 정보 등을 포함한다.

### 5.2 프로세스 상태와 `fork()`

프로세스는 실행 가능, 실행 중, 대기, 정지, 좀비 등 상태를 가진다.

`fork()` 시스템 호출:

- 부모 프로세스에서 자식 프로세스를 만든다.
- 리눅스는 Copy-on-Write 방식을 사용해 처음부터 전체 메모리를 복사하지 않는다.
- 자식이 메모리를 수정할 때 실제 복사가 발생한다.

### 5.3 스케줄링

스케줄링은 실행 가능한 태스크 중 다음에 CPU를 사용할 태스크를 선택하는 과정이다.

정책:

| 정책 | 의미 |
|---|---|
| `SCHED_OTHER` | 일반 time-sharing 정책. 일반 프로세스에 사용 |
| `SCHED_FIFO` | 실시간 FIFO 정책. 같은 우선순위에서는 자발적 양보/블록/종료 전까지 계속 실행 |
| `SCHED_RR` | 실시간 Round-Robin 정책. 같은 우선순위 태스크끼리 time slice를 나눠 실행 |

[수정] 원문에는 `SCHED_RR`와 `SCHED_FIFO` 설명이 혼동될 수 있다. 핵심은 `FIFO`는 같은 우선순위에서 time slice가 없고, `RR`은 같은 우선순위에서 라운드로빈 time slice가 있다는 점이다.

문맥 교환(Context Switch):

- CPU가 실행 중인 태스크의 레지스터/상태를 저장하고 다른 태스크 상태를 복원하는 과정
- 스케줄링 결정 후 발생한다.

### 5.4 메모리 관리

페이지:

- MMU가 관리하는 기본 단위
- 크기는 아키텍처와 커널 설정에 따라 달라진다.
- `struct page`는 `<linux/mm.h>` 관련 구조체다.

리눅스 주소:

- User virtual address
- Kernel virtual/logical address
- Physical address
- Bus address

커널 메모리 할당:

| 함수 | 용도 |
|---|---|
| `kmalloc()` | 물리적으로 연속된 작은 메모리 할당에 주로 사용 |
| `kfree()` | `kmalloc()`으로 할당한 메모리 해제 |
| `vmalloc()` | 가상 주소상 연속된 큰 메모리 할당에 사용. 물리적으로는 불연속 가능 |
| `vfree()` | `vmalloc()` 메모리 해제 |

### 5.5 파일 시스템과 VFS

리눅스는 많은 것을 파일로 추상화한다.

VFS(Virtual File System):

- ext2/ext3/ext4, NFS, tmpfs, procfs, sysfs 등 다양한 파일 시스템을 같은 인터페이스로 다루게 해준다.
- 디바이스 파일도 VFS 경로를 통해 접근한다.

주요 객체:

| 객체 | 의미 |
|---|---|
| super block | 파일 시스템 전체 정보 |
| inode | 파일 자체의 메타데이터 |
| file | 열린 파일 인스턴스 |
| dentry | 경로명과 inode 연결 캐시 |

파일 시스템 예:

- `ext2`, `ext3`, `ext4`
- `cramfs`
- `xfs`
- `jffs2`

### 5.6 커널과 디바이스 통신

디바이스 드라이버는 사용자 프로그램이 직접 호출하지 않는다.

일반 흐름:

```text
user app
  -> open/read/write/ioctl
  -> VFS
  -> device file inode
  -> file_operations
  -> driver method
  -> hardware
```

디바이스 드라이버 관점:

- 하드웨어 레지스터 설정
- 인터럽트 처리
- 사용자 공간과 데이터 복사
- 동시 접근 제어
- 장치 상태 관리

---

## 6. 주요 커널 함수

| 함수 | 내용 | 최신 헤더 참고 |
|---|---|---|
| `printk()` | 커널 로그 출력 | `<linux/kernel.h>` |
| `pr_info()` | 정보 로그 출력 | `<linux/printk.h>` |
| `pr_err()` | 에러 로그 출력 | `<linux/printk.h>` |
| `pr_warn()` | 경고 로그 출력 | `<linux/printk.h>` |
| `pr_debug()` | 디버그 로그 출력 | `<linux/printk.h>` |
| `kmalloc()` | 커널 메모리 할당 | `<linux/slab.h>` |
| `kfree()` | 커널 메모리 해제 | `<linux/slab.h>` |
| `get_user()` | 사용자 공간 값 1개를 커널로 복사 | `<linux/uaccess.h>` |
| `put_user()` | 커널 값 1개를 사용자 공간으로 복사 | `<linux/uaccess.h>` |
| `copy_from_user()` | 사용자 버퍼에서 커널 버퍼로 복사 | `<linux/uaccess.h>` |
| `copy_to_user()` | 커널 버퍼에서 사용자 버퍼로 복사 | `<linux/uaccess.h>` |
| `access_ok()` | 사용자 포인터 접근 가능성 검사 | `<linux/uaccess.h>` |
| `ioremap()` | I/O 물리 주소를 커널 가상 주소로 매핑 | `<linux/io.h>` |
| `iounmap()` | `ioremap()` 매핑 해제 | `<linux/io.h>` |
| `readb/readw/readl()` | MMIO에서 1/2/4바이트 읽기 | `<linux/io.h>` |
| `writeb/writew/writel()` | MMIO에 1/2/4바이트 쓰기 | `<linux/io.h>` |

`copy_from_user()`와 `copy_to_user()`의 반환값:

- 성공: `0`
- 실패: 복사하지 못한 바이트 수
- 따라서 반환값이 0이 아니면 보통 `-EFAULT` 처리한다.

---

## 7. 커널 모듈

### 7.1 모듈 명령어

| 명령어 | 의미 |
|---|---|
| `lsmod` | 현재 로드된 모듈 목록 |
| `insmod module.ko` | 의존성 자동 해결 없이 모듈 직접 로드 |
| `rmmod module` | 모듈 언로드 |
| `modprobe module` | 의존성을 고려해 모듈 로드 |
| `modprobe -r module` | 의존성을 고려해 모듈 제거 |
| `depmod -a` | 모듈 의존성 데이터베이스 갱신 |
| `modinfo module.ko` | 모듈 정보 확인 |
| `dmesg` | 커널 로그 확인 |

수동 설치 흐름:

```bash
sudo cp led_driver.ko /lib/modules/$(uname -r)/kernel/drivers/
sudo depmod -a
sudo modprobe led_driver
sudo modprobe -r led_driver
```

부팅 시 자동 로드:

- 배포판에 따라 `/etc/modules`, `/etc/modules-load.d/*.conf` 등에 모듈 이름을 적는다.
- `modprobe`가 찾을 수 있도록 `/lib/modules/$(uname -r)` 아래에 모듈을 배치하고 `depmod -a`를 수행한다.

### 7.2 최소 커널 모듈 코드

```c
#include <linux/init.h>
#include <linux/module.h>
#include <linux/printk.h>

MODULE_LICENSE("GPL");
MODULE_AUTHOR("Your Name");
MODULE_DESCRIPTION("Hello kernel module");

static int __init hello_init(void)
{
    pr_info("Hello, World\n");
    return 0;
}

static void __exit hello_exit(void)
{
    pr_info("Goodbye\n");
}

module_init(hello_init);
module_exit(hello_exit);
```

시험 포인트:

- `module_init()`에는 초기화 함수
- `module_exit()`에는 종료 함수
- 초기화 함수는 성공 시 `0`, 실패 시 음수 errno 반환
- `MODULE_LICENSE("GPL")`을 빼면 일부 GPL-only 심볼 사용 제한과 taint 메시지가 생길 수 있다.

### 7.3 외부 모듈 Makefile

네이티브 빌드 예:

```makefile
obj-m := hello_module.o

KDIR := /lib/modules/$(shell uname -r)/build
PWD := $(shell pwd)

default:
	$(MAKE) -C $(KDIR) M=$(PWD) modules

clean:
	$(MAKE) -C $(KDIR) M=$(PWD) clean
```

크로스 빌드 예:

```makefile
obj-m := hello_module.o

KDIR := /path/to/kernel/source
PWD := $(shell pwd)

default:
	$(MAKE) ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- -C $(KDIR) M=$(PWD) modules

clean:
	$(MAKE) -C $(KDIR) M=$(PWD) clean
```

빌드 결과:

- `hello_module.ko`
- `hello_module.mod.c`
- `Module.symvers`
- `modules.order`

---

## 8. 커널 로그 출력

### 8.1 `printk()` 로그 레벨

| 레벨 | 매크로 | 의미 |
|---|---|---|
| `<0>` | `KERN_EMERG` | 시스템이 동작할 수 없는 긴급 상황 |
| `<1>` | `KERN_ALERT` | 즉시 조치 필요 |
| `<2>` | `KERN_CRIT` | 치명적 상태 |
| `<3>` | `KERN_ERR` | 오류 |
| `<4>` | `KERN_WARNING` | 경고 |
| `<5>` | `KERN_NOTICE` | 정상이나 중요한 알림 |
| `<6>` | `KERN_INFO` | 일반 정보 |
| `<7>` | `KERN_DEBUG` | 디버깅 정보 |

예:

```c
printk(KERN_INFO "driver loaded\n");
pr_info("driver loaded\n");
pr_err("failed to request gpio\n");
```

### 8.2 형식 지정자

일반 지정자:

- `%s`: 문자열
- `%d`, `%i`: 부호 있는 정수
- `%u`: 부호 없는 정수
- `%x`, `%X`: 16진수
- `%ld`, `%lld`: long, long long

커널 확장:

- `%p`: 포인터. 보안상 해시 처리될 수 있다.
- `%px`: 실제 포인터 주소 출력
- `%pI4`, `%pI6`: IP 주소 계열 출력
- `%ph`: 바이트 배열 hex dump 형태 출력

주의:

- 커널에서는 부동소수점 사용이 제한적이며 일반적으로 피한다.
- 로그를 과도하게 찍으면 성능과 타이밍에 영향을 준다.
- 개행 `\n`을 넣어야 로그가 보기 좋게 출력된다.
- `/proc/kmsg` 또는 `dmesg`로 커널 로그를 확인한다.

---

## 9. 캐릭터 디바이스 드라이버

### 9.1 캐릭터 디바이스란

캐릭터 디바이스는 데이터를 바이트 스트림처럼 처리하는 장치다.

예:

- 키보드
- 시리얼 포트
- GPIO 제어 장치 파일
- 간단한 센서/LED 드라이버

특징:

- 순차 접근 성격이 강하다.
- 데이터가 보존될 수도 있고 보존되지 않을 수도 있다.
- 데이터 발생 시점과 끝을 애플리케이션이 명확히 알기 어려울 수 있다.
- 사용자 공간에서는 `/dev/xxx` 장치 파일로 접근한다.

### 9.2 Major/Minor 번호

디바이스 파일은 major/minor 번호를 가진다.

- major: 어떤 드라이버가 처리할지 식별
- minor: 같은 드라이버 안에서 어떤 장치 인스턴스인지 식별

`dev_t`:

- major/minor를 합친 커널 자료형
- `MKDEV(major, minor)`로 생성
- `MAJOR(dev)`, `MINOR(dev)`로 분리

### 9.3 장치 파일 생성 흐름

수동 생성:

```bash
sudo mknod /dev/mydev c <major> <minor>
sudo chmod 666 /dev/mydev
```

자동 생성 흐름:

```text
alloc_chrdev_region()
  -> cdev_init()
  -> cdev_add()
  -> class_create()
  -> device_create()
  -> udev가 /dev 노드 생성
```

정리 순서:

```text
device_destroy()
  -> class_destroy()
  -> cdev_del()
  -> unregister_chrdev_region()
```

중요 API:

| API | 의미 |
|---|---|
| `register_chrdev_region()` | 정해진 major/minor 범위를 등록 |
| `alloc_chrdev_region()` | major 번호를 동적으로 할당 |
| `cdev_init()` | `struct cdev`와 `file_operations` 연결 |
| `cdev_add()` | 커널에 캐릭터 디바이스 등록 |
| `cdev_del()` | 캐릭터 디바이스 제거 |
| `class_create()` | `/sys/class/<name>` 클래스 생성 |
| `device_create()` | sysfs 장치와 `/dev` 노드 생성 유도 |
| `device_destroy()` | 장치 노드 제거 |
| `class_destroy()` | 클래스 제거 |
| `unregister_chrdev_region()` | 디바이스 번호 반환 |

### 9.4 캐릭터 디바이스 등록 코드 골격

```c
#include <linux/cdev.h>
#include <linux/device.h>
#include <linux/fs.h>
#include <linux/init.h>
#include <linux/module.h>

#define DEVICE_NAME "mydev"
#define CLASS_NAME  "myclass"

static dev_t devno;
static struct cdev my_cdev;
static struct class *my_class;

static int my_open(struct inode *inode, struct file *filp)
{
    return 0;
}

static int my_release(struct inode *inode, struct file *filp)
{
    return 0;
}

static const struct file_operations my_fops = {
    .owner = THIS_MODULE,
    .open = my_open,
    .release = my_release,
};

static int __init my_init(void)
{
    int ret;

    ret = alloc_chrdev_region(&devno, 0, 1, DEVICE_NAME);
    if (ret)
        return ret;

    cdev_init(&my_cdev, &my_fops);
    my_cdev.owner = THIS_MODULE;

    ret = cdev_add(&my_cdev, devno, 1);
    if (ret)
        goto err_unregister;

    my_class = class_create(CLASS_NAME);
    if (IS_ERR(my_class)) {
        ret = PTR_ERR(my_class);
        goto err_cdev;
    }

    if (IS_ERR(device_create(my_class, NULL, devno, NULL, DEVICE_NAME))) {
        ret = -ENOMEM;
        goto err_class;
    }

    return 0;

err_class:
    class_destroy(my_class);
err_cdev:
    cdev_del(&my_cdev);
err_unregister:
    unregister_chrdev_region(devno, 1);
    return ret;
}

static void __exit my_exit(void)
{
    device_destroy(my_class, devno);
    class_destroy(my_class);
    cdev_del(&my_cdev);
    unregister_chrdev_region(devno, 1);
}

module_init(my_init);
module_exit(my_exit);
MODULE_LICENSE("GPL");
```

[주의] `class_create()` 시그니처는 커널 버전에 따라 다르다. 구버전 코드에서는 `class_create(THIS_MODULE, CLASS_NAME)` 형식을 볼 수 있다.

### 9.5 장치 파일 권한

장치 파일은 기본적으로 보수적인 권한으로 생성될 수 있다.

권한 처리 방법:

- 테스트 중에는 `chmod` 사용
- 제품/드라이버에서는 udev rule 사용
- 커널 class의 `devnode` 콜백으로 mode 지정 가능

예:

```c
static char *my_devnode(const struct device *dev, umode_t *mode)
{
    if (mode)
        *mode = 0666;
    return NULL;
}
```

---

## 10. `file_operations`

### 10.1 역할

`struct file_operations`는 사용자 공간 시스템 호출과 커널 드라이버 함수를 연결하는 함수 포인터 테이블이다.

사용자 공간:

```c
fd = open("/dev/mydev", O_RDWR);
read(fd, buf, len);
write(fd, buf, len);
ioctl(fd, CMD, arg);
close(fd);
```

커널 공간:

```c
static const struct file_operations my_fops = {
    .owner = THIS_MODULE,
    .open = my_open,
    .read = my_read,
    .write = my_write,
    .unlocked_ioctl = my_ioctl,
    .release = my_release,
};
```

자주 쓰는 멤버:

| 멤버 | 의미 |
|---|---|
| `.owner` | 보통 `THIS_MODULE`. 모듈 참조 카운트 관리 |
| `.open` | 장치 파일 열기 |
| `.release` | 장치 파일 닫기 |
| `.read` | 디바이스에서 사용자 공간으로 데이터 전달 |
| `.write` | 사용자 공간에서 디바이스로 데이터 전달 |
| `.llseek` | 파일 위치 이동 |
| `.poll` | `poll/select/epoll` 지원 |
| `.unlocked_ioctl` | 장치별 제어 명령 |
| `.mmap` | 디바이스 메모리 매핑 |
| `.fasync` | 비동기 시그널 통지 |

### 10.2 `open()`과 `release()`

`open()`에서 하는 일:

- 하드웨어 준비 상태 확인
- 처음 열릴 때 장치 초기화
- minor 번호에 따라 다른 동작 선택
- 프로세스별 데이터 구조 할당
- `filp->private_data` 설정
- 이미 사용 중인 장치면 `-EBUSY` 반환 가능

`release()`에서 하는 일:

- `open()`에서 할당한 자원 해제
- 마지막 close일 때 장치 종료
- `private_data` 해제

대표 errno:

| errno | 의미 |
|---|---|
| `-ENODEV` | 장치 없음 |
| `-ENOMEM` | 커널 메모리 부족 |
| `-EBUSY` | 장치 사용 중 |
| `-EINVAL` | 잘못된 인자 |

### 10.3 `struct file`

`struct file`은 열린 파일 하나에 대응하는 커널 객체다.

중요 필드:

| 필드 | 의미 |
|---|---|
| `f_op` | 현재 파일에 연결된 `file_operations` |
| `f_mode` | 읽기/쓰기 모드. `FMODE_READ`, `FMODE_WRITE` |
| `f_pos` | 현재 파일 offset |
| `f_flags` | `O_RDONLY`, `O_NONBLOCK`, `O_SYNC` 같은 open flag |
| `private_data` | 드라이버가 파일 인스턴스별 데이터를 저장하는 포인터 |

시험 포인트:

- 사용자 `open()`의 pathname과 flags가 그대로 드라이버 `open()` 인자로 들어오는 것은 아니다.
- 커널 VFS가 inode/file 구조체를 준비한 뒤 드라이버 콜백을 호출한다.
- 드라이버는 `inode`에서 major/minor 정보를 확인할 수 있다.
- 드라이버는 `filp->private_data`를 사용해 open별 상태를 저장할 수 있다.

---

## 11. `read()`와 `write()`

### 11.1 함수 시그니처

```c
static ssize_t my_read(struct file *filp,
                       char __user *buf,
                       size_t count,
                       loff_t *ppos);

static ssize_t my_write(struct file *filp,
                        const char __user *buf,
                        size_t count,
                        loff_t *ppos);
```

### 11.2 사용자 공간과 커널 공간 복사

사용자 -> 커널:

```c
if (copy_from_user(kbuf, buf, count))
    return -EFAULT;
```

커널 -> 사용자:

```c
if (copy_to_user(buf, kbuf, count))
    return -EFAULT;
```

값 하나만 복사:

```c
get_user(x, user_ptr);
put_user(x, user_ptr);
```

### 11.3 `write()` 구현 핵심

`write()`는 사용자 버퍼의 데이터를 디바이스 또는 커널 버퍼로 가져오는 함수다.

흐름:

```text
write(fd, user_buf, count)
  -> my_write()
  -> copy_from_user()
  -> 입력값 검증
  -> 장치 상태 확인
  -> 하드웨어 레지스터 쓰기 또는 내부 버퍼 저장
  -> 처리한 바이트 수 반환
```

대표 에러:

| errno | 의미 |
|---|---|
| `-EAGAIN` | `O_NONBLOCK`인데 즉시 처리 불가 |
| `-EIO` | I/O 오류 |
| `-EFAULT` | 사용자 버퍼 주소 오류 |
| `-ENOSPC` | 저장 공간 부족 |

골격:

```c
static ssize_t my_write(struct file *filp,
                        const char __user *buf,
                        size_t count,
                        loff_t *ppos)
{
    char kbuf[32];
    size_t len = min(count, sizeof(kbuf) - 1);

    if (!device_ready()) {
        if (filp->f_flags & O_NONBLOCK)
            return -EAGAIN;
        /* wait_event_interruptible() 등으로 대기 */
    }

    if (copy_from_user(kbuf, buf, len))
        return -EFAULT;

    kbuf[len] = '\0';
    /* 하드웨어 제어 */

    return len;
}
```

### 11.4 `read()` 구현 핵심

`read()`는 디바이스 또는 커널 버퍼의 데이터를 사용자 버퍼로 전달하는 함수다.

흐름:

```text
read(fd, user_buf, count)
  -> my_read()
  -> 데이터 준비 여부 확인
  -> 필요 시 blocking 또는 -EAGAIN
  -> 하드웨어/버퍼에서 데이터 읽기
  -> copy_to_user()
  -> 전달한 바이트 수 반환
```

골격:

```c
static ssize_t my_read(struct file *filp,
                       char __user *buf,
                       size_t count,
                       loff_t *ppos)
{
    char kbuf[] = "hello\n";
    size_t len = strlen(kbuf);

    if (*ppos >= len)
        return 0;

    if (count > len - *ppos)
        count = len - *ppos;

    if (copy_to_user(buf, kbuf + *ppos, count))
        return -EFAULT;

    *ppos += count;
    return count;
}
```

### 11.5 Blocking과 Non-blocking

`O_NONBLOCK`이 없는 경우:

- 데이터가 준비될 때까지 대기 가능
- `wait_event_interruptible()` 같은 wait queue 사용 가능

`O_NONBLOCK`인 경우:

- 즉시 처리할 수 없으면 `-EAGAIN` 반환

시험 포인트:

- `filp->f_flags & O_NONBLOCK`으로 non-blocking 여부 확인
- 인터럽트 컨텍스트와 프로세스 컨텍스트 경쟁을 고려해야 한다.
- 다중 프로세스 동시 접근에는 mutex/spinlock/wait queue 등이 필요하다.

---

## 12. 하드웨어 I/O 함수

포트 매핑 I/O:

| 함수 | 의미 |
|---|---|
| `inb()` | 포트에서 1바이트 읽기 |
| `inw()` | 포트에서 2바이트 읽기 |
| `inl()` | 포트에서 4바이트 읽기 |
| `outb()` | 포트에 1바이트 쓰기 |
| `outw()` | 포트에 2바이트 쓰기 |
| `outl()` | 포트에 4바이트 쓰기 |
| `insb/insw/insl()` | 포트에서 여러 바이트/워드/롱 읽기 |
| `outsb/outsw/outsl()` | 포트로 여러 바이트/워드/롱 쓰기 |

Memory-mapped I/O:

| 함수 | 의미 |
|---|---|
| `readb()` | MMIO에서 1바이트 읽기 |
| `readw()` | MMIO에서 2바이트 읽기 |
| `readl()` | MMIO에서 4바이트 읽기 |
| `writeb()` | MMIO에 1바이트 쓰기 |
| `writew()` | MMIO에 2바이트 쓰기 |
| `writel()` | MMIO에 4바이트 쓰기 |
| `memset_io()` | I/O 메모리 영역 초기화 |
| `memcpy_fromio()` | I/O 메모리에서 일반 메모리로 복사 |
| `memcpy_toio()` | 일반 메모리에서 I/O 메모리로 복사 |

[보충] ARM/Raspberry Pi GPIO처럼 레지스터가 메모리 주소에 매핑된 장치는 보통 MMIO 방식이다.

---

## 13. `ioctl`

### 13.1 목적

`ioctl`은 `read/write`로 표현하기 어려운 장치별 제어 명령을 처리한다.

예:

- LED ON/OFF
- 장치 상태 조회
- 샘플링 주기 설정
- 모드 변경
- 구조체 형태의 설정값 전달

사용자 공간:

```c
ioctl(fd, CMD, arg);
```

커널 공간:

```c
static long my_ioctl(struct file *filp,
                     unsigned int cmd,
                     unsigned long arg)
{
    return 0;
}
```

`file_operations` 연결:

```c
static const struct file_operations my_fops = {
    .owner = THIS_MODULE,
    .unlocked_ioctl = my_ioctl,
};
```

### 13.2 cmd 비트 필드

`cmd`에는 다음 정보가 들어간다.

| 필드 | 의미 |
|---|---|
| TYPE | 매직 넘버. 드라이버 그룹 식별 |
| NR | 명령 번호 |
| SIZE | 전달 데이터 크기 |
| DIR | 데이터 전송 방향 |

해석 매크로:

| 매크로 | 의미 |
|---|---|
| `_IOC_TYPE(cmd)` | 매직 넘버 |
| `_IOC_NR(cmd)` | 명령 번호 |
| `_IOC_SIZE(cmd)` | 데이터 크기 |
| `_IOC_DIR(cmd)` | 방향 |

작성 매크로:

| 매크로 | 의미 |
|---|---|
| `_IO(type, nr)` | 부가 데이터 없는 명령 |
| `_IOR(type, nr, data_type)` | user가 driver에서 데이터를 읽는 명령 |
| `_IOW(type, nr, data_type)` | user가 driver로 데이터를 쓰는 명령 |
| `_IOWR(type, nr, data_type)` | 양방향 명령 |

[주의] `_IOR`/`_IOW`의 R/W는 사용자 공간 관점이다.

- `_IOR`: 사용자 프로그램이 읽음. 커널 드라이버는 `copy_to_user()`를 수행하는 경우가 많다.
- `_IOW`: 사용자 프로그램이 씀. 커널 드라이버는 `copy_from_user()`를 수행하는 경우가 많다.

### 13.3 ioctl 명령 예

```c
#ifndef _SK_H_
#define _SK_H_

#include <linux/ioctl.h>

#define SK_MAGIC 'k'
#define SK_MAXNR 5

typedef struct {
    unsigned long size;
    unsigned char buf[128];
} __attribute__((packed)) sk_info;

#define SK_LED_OFF   _IO(SK_MAGIC, 0)
#define SK_LED_ON    _IO(SK_MAGIC, 1)
#define SK_GETSTATE  _IOR(SK_MAGIC, 2, int)
#define SK_READ      _IOR(SK_MAGIC, 3, sk_info)
#define SK_WRITE     _IOW(SK_MAGIC, 4, sk_info)
#define SK_RW        _IOWR(SK_MAGIC, 5, sk_info)

#endif
```

### 13.4 ioctl 구현 골격

```c
static long sk_ioctl(struct file *filp, unsigned int cmd, unsigned long arg)
{
    sk_info info;

    if (_IOC_TYPE(cmd) != SK_MAGIC)
        return -EINVAL;

    if (_IOC_NR(cmd) > SK_MAXNR)
        return -EINVAL;

    switch (cmd) {
    case SK_LED_OFF:
        /* LED off */
        break;

    case SK_LED_ON:
        /* LED on */
        break;

    case SK_READ:
        memset(&info, 0, sizeof(info));
        info.size = 1;
        info.buf[0] = 1;
        if (copy_to_user((void __user *)arg, &info, sizeof(info)))
            return -EFAULT;
        break;

    case SK_WRITE:
        if (copy_from_user(&info, (void __user *)arg, sizeof(info)))
            return -EFAULT;
        /* info 기반 제어 */
        break;

    default:
        return -ENOTTY;
    }

    return 0;
}
```

시험 포인트:

- 매직 넘버 검사
- 명령 번호 범위 검사
- `arg`가 포인터일 수 있으므로 직접 역참조 금지
- 사용자 포인터는 `copy_to_user/copy_from_user` 사용
- 알 수 없는 명령은 보통 `-ENOTTY` 또는 `-EINVAL`

---

## 14. BCM2711/Raspberry Pi 4 GPIO 주소

### 14.1 주소 체계

BCM2711 문서의 GPIO register base:

```text
legacy peripheral address: 0x7E200000
```

Raspberry Pi 4 Low Peripheral mode에서 ARM이 보는 물리 주소:

```text
ARM physical peripheral base: 0xFE000000
GPIO offset:                  0x00200000
GPIO ARM physical address:    0xFE200000
```

정리:

```text
문서상 GPIO 주소 0x7E200000
  = legacy peripheral base 0x7E000000 + GPIO offset 0x00200000

ARM physical GPIO 주소
  = 0xFE000000 + 0x00200000
  = 0xFE200000
```

확인 명령:

```bash
grep gpio /proc/iomem
```

예상 출력:

```text
fe200000.gpio gpio@7e200000
```

의미:

- `gpio@7e200000`: 장치 트리/문서상 bus 또는 legacy 주소 표현
- `fe200000.gpio`: ARM 물리 주소 관점에서 매핑된 GPIO

### 14.2 BCM2711 주소 관련 핵심

ARM physical address:

- RAM은 물리 주소 `0x0_0000_0000`부터 시작한다.
- ARM이 접근하는 주변장치 영역은 Low Peripheral mode에서 `0x0_FC00_0000` ~ `0x0_FFFF_FFFF` 쪽에 보인다.
- main peripherals와 local peripherals 영역이 나뉜다.

Legacy master address:

- DMA 엔진 등 일부 버스 master가 주변장치를 접근할 때 legacy 주소를 사용할 수 있다.
- 문서에 `0x7Enn_nnnn`으로 적힌 주변장치는 Low Peripheral mode에서 ARM에게 `0x0_FEnn_nnnn` 형태로 보일 수 있다.
- `/dev/mem` 또는 커널 `ioremap()`에는 CPU/ARM physical address 기준 주소를 사용해야 한다.

### 14.3 GPIO 레지스터 매핑

예:

```c
#define BCM_IO_BASE 0xFE000000  /* Raspberry Pi 4 */
#define GPIO_BASE   (BCM_IO_BASE + 0x200000)
#define GPIO_SIZE   256
```

[수정] 원문 주석의 `0x7E2000B0 - 0x7E2000000`은 0이 하나 더 들어간 오타다. GPIO 레지스터 사용 범위는 대략 `0x7E200000`부터 `0x7E2000B0`까지이며, 계산하면 `0xB4` 바이트다. 실습에서는 페이지 정렬과 여유를 위해 256바이트 또는 페이지 크기 단위로 매핑할 수 있다.

### 14.4 GPIO Function Select

BCM2711 GPIO는 각 핀의 기능을 FSEL 필드로 설정한다.

- 각 GPIO 핀은 최소 두 개 이상의 alternate function을 가질 수 있다.
- `GPFSELn` 레지스터에서 각 핀은 3비트를 사용한다.
- `000`: input
- `001`: output
- 그 외 값: ALT 기능
- reset 후 기본값은 보통 GPIO input이다.

GPIO 18 예:

```text
GPIO 18
  -> GPFSEL1 사용
  -> 18 % 10 = 8
  -> bit 26:24 사용
```

매크로 예:

```c
static volatile unsigned int *gpio;

#define GPIO_IN(g)   (*(gpio + ((g) / 10)) &= ~(7 << (((g) % 10) * 3)))
#define GPIO_OUT(g)  (*(gpio + ((g) / 10)) |=  (1 << (((g) % 10) * 3)))
#define GPIO_SET(g)  (*(gpio + 7)  = (1 << (g)))
#define GPIO_CLR(g)  (*(gpio + 10) = (1 << (g)))
#define GPIO_GET(g)  (*(gpio + 13) & (1 << (g)))
```

레지스터 offset 의미:

| 매크로 | 레지스터 의미 |
|---|---|
| `gpio + 0..5` | `GPFSEL0..5` |
| `gpio + 7` | `GPSET0` |
| `gpio + 10` | `GPCLR0` |
| `gpio + 13` | `GPLEV0` |

[보충] `gpio`가 `unsigned int *`이므로 `gpio + 7`은 바이트 기준 `7 * 4 = 28 = 0x1C` offset이다.

---

## 15. 커널 GPIO API

### 15.1 legacy integer GPIO API

헤더:

```c
#include <linux/gpio.h>
```

GPIO 요청:

```c
int gpio_request(unsigned gpio, const char *label);
void gpio_free(unsigned gpio);
```

GPIO 방향:

```c
int gpio_direction_input(unsigned gpio);
int gpio_direction_output(unsigned gpio, int value);
```

GPIO 값:

```c
void gpio_set_value(unsigned gpio, int value);
int gpio_get_value(unsigned gpio);
```

여러 GPIO 요청:

```c
struct gpio {
    unsigned gpio;
    unsigned long flags;
    const char *label;
};
```

예:

```c
static struct gpio devices[] = {
    { GPIO_LED,   GPIOF_OUT_INIT_HIGH, "LED"   },
    { GPIO_MOTOR, GPIOF_OUT_INIT_HIGH, "MOTOR" },
};

ret = gpio_request_array(devices, ARRAY_SIZE(devices));
if (ret)
    return ret;

gpio_set_value(devices[0].gpio, cmd);
gpio_set_value(devices[1].gpio, cmd);

gpio_free_array(devices, ARRAY_SIZE(devices));
```

[주의] legacy integer GPIO API는 최신 커널에서 descriptor 기반 GPIO API(`gpiod_*`)로 대체되는 추세다. 수업/실습 코드가 legacy API를 쓰면 시험에서는 수업 기준을 따르되, 실무 보충으로는 descriptor API를 알아두면 좋다.

### 15.2 GPIO 번호 확인

직접 레지스터 접근과 커널 GPIO API는 핀 번호 해석이 다를 수 있다.

확인 명령:

```bash
sudo cat /sys/kernel/debug/gpio
```

주의:

- 물리 핀 번호, BCM GPIO 번호, 커널 내부 GPIO 번호가 다를 수 있다.
- Raspberry Pi 문서의 GPIO 18은 보드 물리 핀 번호가 아니라 BCM GPIO 번호다.

### 15.3 GPIO interrupt

GPIO 핀을 IRQ 번호로 변환:

```c
int irq = gpio_to_irq(gpio);
```

IRQ 타입 설정:

```c
irq_set_irq_type(irq, IRQ_TYPE_EDGE_RISING);
irq_set_irq_type(irq, IRQ_TYPE_EDGE_FALLING);
irq_set_irq_type(irq, IRQ_TYPE_EDGE_BOTH);
irq_set_irq_type(irq, IRQ_TYPE_LEVEL_HIGH);
irq_set_irq_type(irq, IRQ_TYPE_LEVEL_LOW);
```

인터럽트 핸들러 등록:

```c
ret = request_irq(irq, handler, IRQF_TRIGGER_RISING, "my_irq", dev_id);
```

해제:

```c
free_irq(irq, dev_id);
```

Polling vs interrupt:

- polling: 주기적으로 상태를 읽는다. 단순하지만 CPU 낭비가 생긴다.
- interrupt: 이벤트 발생 시 커널이 핸들러를 호출한다. 효율적이지만 동시성/컨텍스트 제약을 고려해야 한다.

### 15.4 애플리케이션으로 시그널 전달

커널에서 이벤트를 직접 처리하지 않고 사용자 애플리케이션에 알릴 수 있다.

일반 흐름:

```text
사용자 앱이 자신의 PID를 write/ioctl로 드라이버에 전달
  -> 드라이버가 PID 저장
  -> 인터럽트 발생
  -> 드라이버가 해당 task_struct 찾기
  -> send_sig_info() 등으로 signal 전달
  -> 사용자 앱의 signal handler 실행
```

PID 관련 함수:

| 함수 | 의미 |
|---|---|
| `task_pid()` | 태스크의 PID 구조체 반환 |
| `pid_task()` | `pid`에 해당하는 태스크 반환 |
| `task_tgid()` | 스레드 그룹 리더 PID |
| `task_pgrp()` | 프로세스 그룹 리더 PID |
| `task_session()` | 세션 리더 PID |
| `pid_nr()` | 전역 PID 번호 |
| `pid_vnr()` | 가상 PID 번호 |
| `pid_nr_ns()` | 특정 네임스페이스의 PID 번호 |
| `find_vpid()` | 가상 PID에 해당하는 `struct pid` 반환 |
| `find_pid_ns()` | 특정 네임스페이스에서 PID 검색 |

---

## 16. 시스템 호출 함수

시스템 호출은 사용자 공간에서 커널 기능을 요청하는 공식 진입점이다.

흐름:

```text
user function
  -> C library wrapper
  -> syscall instruction 또는 software interrupt
  -> syscall number 확인
  -> sys_* 또는 __do_sys_* 계열 커널 함수 호출
```

시스템 호출 번호:

- 아키텍처마다 다르다.
- ARM/ARM64에서는 커널 소스의 `include/uapi/asm-generic/unistd.h`, `arch/arm64/include/asm/unistd.h` 등과 생성된 syscall table을 확인한다.

원문 예시의 정정:

```c
/* include/uapi/asm-generic/unistd.h */
#define __NR_mycall 451
__SYSCALL(__NR_mycall, sys_mycall)

/* 전체 syscall 수를 하나 증가 */
#define __NR_syscalls 452
```

[보충] 실제 최신 커널에 시스템 호출을 추가하려면 단순히 번호만 추가하는 것으로 끝나지 않는다. 아키텍처별 테이블, 함수 선언, 구현 파일, Kconfig/Makefile, ABI 호환성, userspace 헤더 반영까지 맞춰야 한다.

---

## 17. 임베디드 리눅스 개요

### 17.1 임베디드 리눅스를 선택하는 이유

장점:

- 멀티태스킹 지원
- TCP/IP 등 인터넷 프로토콜 스택 지원
- USB, Wi-Fi, Bluetooth, 저장장치, 멀티미디어 장치 지원
- ARM, MIPS, x86, PowerPC, RISC-V 등 다양한 아키텍처 지원
- 오픈소스라 필요에 따라 수정/삭제 가능
- 커뮤니티가 활발해 최신 하드웨어와 표준 반영이 빠르다.
- 특정 벤더에 종속되지 않는다.

고려 사항:

- 하드웨어 사양이 리눅스 실행에 충분한가
- 커널, 부트로더, 장치 트리, 드라이버를 이해할 수 있는가
- 실시간성이 강하게 필요한 시스템인가
- 인증, 규제, 안전 표준이 필요한 제품인가
- GPL 등 오픈소스 라이선스를 준수할 수 있는가

### 17.2 주요 오픈소스 프로젝트

- Linux kernel
- U-Boot
- BusyBox
- Buildroot
- Yocto Project
- GNU toolchain
- OpenEmbedded

### 17.3 CPU/SoC/보드/상용 벤더

CPU 아키텍처:

- ARM, ARM64
- x86, x86_64
- MIPS
- PowerPC
- RISC-V

SoC 벤더:

- Broadcom
- Intel
- Microchip
- NXP
- Qualcomm
- Texas Instruments

보드:

- Raspberry Pi
- BeagleBoard
- BeagleBone

상용 리눅스 공급 업체:

- Siemens
- Timesys
- Wind River

### 17.4 임베디드 리눅스 5요소

원문 4요소에 애플리케이션까지 합치면 실제 제품 관점의 5요소가 된다.

| 요소 | 역할 |
|---|---|
| 툴체인 | 타깃용 컴파일러, 링커, 라이브러리, 디버거 |
| 부트로더 | 보드 초기화, 커널 로드, bootargs/DTB 전달 |
| 커널 | 자원 관리, 하드웨어 제어, 시스템 호출 제공 |
| 루트 파일시스템 | `/bin`, `/sbin`, `/lib`, `/etc`, init, shell, 라이브러리 |
| 애플리케이션 | 제품 기능을 구현하는 사용자 프로그램 |

### 17.5 오픈소스 라이선스

| 라이선스 계열 | 특징 |
|---|---|
| GPL | Copyleft. 배포 시 소스 공개 의무가 강함 |
| LGPL | 라이브러리 링크에 대해 GPL보다 완화 |
| BSD/MIT/Apache | Permissive. 고지 조건 중심, 상용 사용이 비교적 자유로움 |

시험 포인트:

- 커널은 GPL v2
- 사용자 공간 프로그램과 커널 모듈의 라이선스 영향은 구분해서 봐야 한다.
- 제품 배포 시 소스 공개, 라이선스 고지, 변경 사항 공개 의무를 검토해야 한다.

### 17.6 임베디드 리눅스 하드웨어 조건

필요 조건:

- 리눅스가 지원하는 CPU 아키텍처
- 충분한 RAM. 원문 기준 최소 16MiB 이상
- 비휘발성 메모리. 간단한 라우터/웹캠은 8MiB 수준에서도 가능할 수 있으나 현대 시스템은 더 많이 필요하다.
- 디버그 포트. UART 시리얼 콘솔이 매우 중요하다.
- 소프트웨어 로딩 방법. SD/eMMC/SPI flash/NAND/USB/network/JTAG 등

[보충] 실무에서는 부팅 로그를 보기 위한 UART, 복구 가능한 부트 경로, JTAG/SWD 같은 디버깅 경로가 개발 효율에 큰 영향을 준다.

---

## 18. QEMU

QEMU(Quick Emulator):

- Fabrice Bellard가 개발한 오픈소스 에뮬레이터/가상화 도구
- 다른 CPU 아키텍처용 운영체제와 프로그램을 호스트에서 실행할 수 있다.
- 전체 시스템 에뮬레이션과 사용자 모드 에뮬레이션을 모두 지원한다.
- KVM과 함께 사용하면 하드웨어 가상화를 활용할 수 있다.

패키지 예:

| 패키지 | 대상 |
|---|---|
| `qemu-system-arm` | ARM |
| `qemu-system-aarch64` | ARM64 |
| `qemu-system-mips` | MIPS |
| `qemu-system-ppc` | PowerPC |
| `qemu-system-x86_64` | x86_64 |

시험 포인트:

- 실제 보드 없이 커널/루트파일시스템/부트 흐름을 테스트할 수 있다.
- 단, 모든 주변장치가 실제 하드웨어와 동일하게 에뮬레이션되는 것은 아니다.

---

## 19. 툴체인

### 19.1 GNU 툴체인 구성

| 구성 요소 | 역할 |
|---|---|
| binutils | assembler, linker, objdump, objcopy 등 |
| GCC | C/C++ 등 컴파일러 |
| C library | glibc, musl, uClibc-ng 등 |
| GDB | 디버거 |
| headers | 커널/라이브러리 ABI 헤더 |

C 라이브러리는 애플리케이션과 커널 시스템 호출 사이의 주요 인터페이스를 제공한다.

### 19.2 Native toolchain과 Cross toolchain

Native toolchain:

- 빌드하는 시스템과 실행할 시스템이 같은 경우
- 예: Raspberry Pi에서 Raspberry Pi용 프로그램 빌드

Cross toolchain:

- 빌드 머신과 타깃 머신이 다른 경우
- 예: x86_64 PC에서 ARM64 Raspberry Pi용 프로그램 빌드

툴체인 선택 시 확인할 것:

- CPU 아키텍처: ARM, ARM64, MIPS, x86_64 등
- endian: little endian 또는 big endian
- ABI: 함수 호출 규약, 레지스터 사용 규칙, 바이너리 인터페이스
- floating point ABI: hard-float, soft-float 등
- C library: glibc, musl 등
- OS/환경: bare-metal, Linux 등

툴체인 triplet 예:

```text
aarch64-linux-gnu-
arm-linux-gnueabihf-
riscv64-linux-gnu-
x86_64-linux-gnu-
```

### 19.3 툴체인 구하는 방법

- SoC/보드 벤더 제공 SDK
- 리눅스 배포판 패키지
- Yocto SDK
- Buildroot SDK
- 상용 벤더 제공 툴체인
- 직접 crosstool-NG 등으로 빌드

---

## 20. 부트로더

### 20.1 부트로더 역할

부트로더는 시스템 시작 후 운영체제 커널을 로드하는 프로그램이다.

역할:

- CPU/SoC 초기화
- clock 설정
- pinmux 설정
- DRAM controller 초기화
- UART 같은 디버그 장치 초기화
- 부트 장치 선택
- 커널 이미지 로드
- DTB와 initramfs 로드
- bootargs 설정
- 커널 entry point로 제어권 전달

커널이 시작되면 부트로더 자신은 더 이상 실행되지 않는다.

부가 기능:

- 부트 구성 업데이트
- 새로운 이미지 다운로드
- 플래시 메모리 관리
- 진단 기능
- 시리얼 콘솔 명령줄 제공
- 네트워크 부트(TFTP/NFS 등)

### 20.2 부트로더의 세부 기능

타깃 시스템 초기화:

- 전원 입력 후 정상 동작 환경 구성
- 불필요한 하드웨어 정지
- clock 설정
- 메모리 컨트롤러 설정
- 필요 시 MMU/MPU 설정
- 재배치와 스택 설정
- C 런타임에 필요한 BSS/data 초기화
- 예외 벡터/핸들러 설정

동작 환경 설정:

- 부트 디바이스 선택
- 네트워크 부트 IP 설정
- 커널 command line 설정
- rootfs 위치 설정

운영체제 부팅:

- 플래시/SD/eMMC/network에서 커널 이미지를 DRAM으로 복사
- DTB와 initramfs를 적절한 위치에 배치
- 커널로 jump

플래시 메모리 관리:

- 부트로더 저장
- 환경 변수 저장
- 커널/rootfs 이미지 저장

모니터 기능:

- POST(Power-On Self Test)
- 메모리 검사
- 장치 진단

### 20.3 일반 부트 순서

정리된 일반 순서:

```text
Power On / Reset
  -> SoC internal ROM code 실행
  -> boot mode pin/eFuse/strap 확인
  -> SD/eMMC/SPI/NAND 등에서 초기 부트로더 검색
  -> optional TPL 또는 SPL을 내부 SRAM에 적재
  -> 초기 부트로더 실행
  -> clock/pinmux/UART/DRAM controller 초기화
  -> DRAM 사용 가능
  -> U-Boot proper를 DRAM에 적재
  -> U-Boot proper 실행
  -> kernel image, FDT(DTB), initramfs를 DRAM에 배치
  -> bootargs 설정
  -> kernel entry point로 jump
  -> kernel이 DTB를 읽고 하드웨어 구성 파악
  -> initramfs 또는 rootfs mount
  -> /init, systemd, init 실행
  -> 사용자 공간 부팅 완료
```

[주의] 원문 흐름에는 SPL 이후 TPL이 나오는 형태가 있지만, U-Boot 용어에서 TPL은 보통 SPL보다 더 앞단의 초소형 로더로 사용된다. 시험에서는 강의 흐름을 기억하되, 일반적인 U-Boot 표현은 `ROM -> TPL(optional) -> SPL -> U-Boot proper -> kernel`로 정리한다.

### 20.4 부트로더에서 커널로 전달하는 정보

부트로더가 커널에 전달해야 할 수 있는 정보:

- Machine Number: 장치 트리를 쓰지 않는 오래된 ARM 방식에서 SoC/보드 식별
- CPU clock
- 물리 RAM 크기와 위치
- 커널 command line
- DTB 위치와 크기
- initramfs/initrd 위치와 크기
- rootfs 위치

현대 ARM/ARM64 리눅스에서는 장치 트리(DTB)가 핵심이다.

---

## 21. U-Boot

U-Boot:

- 임베디드 분야에서 널리 쓰이는 오픈소스 부트로더
- 다양한 CPU 아키텍처와 장치 지원
- DENX Software Engineering이 주도적으로 유지보수
- ARM 기반 임베디드 Linux 시스템에서 흔히 사용
- 각 보드에 맞는 porting이 필요하다.

U-Boot에서 자주 보는 것:

- `bootcmd`: 자동 부팅 명령
- `bootargs`: 커널 command line
- `loadaddr`: 커널 로드 주소
- `fdt_addr` 또는 `fdt_addr_r`: DTB 로드 주소
- `initrd_addr`: initramfs 로드 주소
- `tftpboot`, `fatload`, `ext4load`: 이미지 로드 명령
- `booti`, `bootm`, `bootz`: 커널 부팅 명령

커널 이미지별 부팅:

| 명령 | 대상 |
|---|---|
| `booti` | ARM64 `Image` |
| `bootz` | ARM zImage |
| `bootm` | uImage/FIT image |

---

## 22. 장치 트리(Device Tree)

### 22.1 개념

장치 트리는 하드웨어 구성을 설명하는 정적 데이터 구조다.

역할:

- CPU, 메모리, 버스, GPIO, UART, I2C, SPI, interrupt controller 등 하드웨어 구성을 커널에 전달
- 커널 코드를 보드별로 하드코딩하지 않도록 분리
- 부트로더가 DTB를 커널에 전달하는 방식이 일반적이다.

파일:

| 확장자 | 의미 |
|---|---|
| `.dts` | Device Tree Source |
| `.dtsi` | include용 Device Tree Source |
| `.dtb` | Device Tree Blob, 바이너리 |
| `.dtbo` | Device Tree Overlay 바이너리 |

소스 위치:

```text
arch/$ARCH/boot/dts/
```

### 22.2 장치 트리 구조

장치 트리는 노드와 프로퍼티로 구성된다.

예:

```dts
/dts-v1/;

/ {
    model = "TI AM335x BeagleBone";
    compatible = "ti,am33xx";
    #address-cells = <1>;
    #size-cells = <1>;

    cpus {
        #address-cells = <1>;
        #size-cells = <0>;

        cpu@0 {
            compatible = "arm,cortex-a8";
            device_type = "cpu";
            reg = <0>;
        };
    };

    memory@80000000 {
        device_type = "memory";
        reg = <0x80000000 0x20000000>; /* 512 MiB */
    };
};
```

64비트 주소 예:

```dts
/ {
    #address-cells = <2>;
    #size-cells = <2>;

    memory@80000000 {
        device_type = "memory";
        reg = <0x00000000 0x80000000 0x00000000 0x80000000>;
    };
};
```

`#address-cells`, `#size-cells`:

- 자식 노드의 `reg` 주소와 크기를 몇 개의 32비트 cell로 표현할지 지정한다.
- 부모 노드의 설정이 자식 노드의 `reg` 해석 방식에 영향을 준다.

### 22.3 주요 프로퍼티

| 프로퍼티 | 의미 |
|---|---|
| `compatible` | 드라이버 매칭에 사용. 보통 `"vendor,device"` 형식 |
| `device_type` | CPU, memory 등 일부 표준 노드 종류 |
| `reg` | 레지스터 또는 메모리 범위. `<주소 크기>` 형식 |
| `status` | `"okay"`, `"disabled"` 등 장치 활성 상태 |
| `interrupt-parent` | 연결된 interrupt controller phandle |
| `interrupts` | 인터럽트 번호/트리거 정보 |
| `interrupt-controller` | 해당 노드가 인터럽트 컨트롤러임을 표시 |
| `#interrupt-cells` | interrupt specifier cell 개수 |

드라이버 매칭:

```c
static const struct of_device_id my_of_match[] = {
    { .compatible = "vendor,mydev" },
    { }
};
MODULE_DEVICE_TABLE(of, my_of_match);
```

시험 포인트:

- 장치 트리는 실행 코드가 아니라 하드웨어 설명 데이터다.
- 커널은 `compatible` 문자열로 적절한 드라이버를 찾는다.
- `reg` 해석에는 부모의 `#address-cells`, `#size-cells`가 중요하다.
- `interrupts`만 있고 `interrupt-parent`가 없으면 부모 노드에서 상속될 수 있다.

---

## 23. 커널 구성과 빌드

### 23.1 커널 모듈과 내장 빌드

임베디드 장치 드라이버는 제품에 따라 모듈보다 커널에 직접 내장하는 경우가 많다.

모듈이 좋은 경우:

- 라이선스나 배포 정책상 분리 필요
- 필수 장치가 아니라 부팅 후 늦게 로드해도 됨
- 정적 링크 시 커널 이미지가 지나치게 커짐
- 개발/디버깅 중 자주 로드/언로드해야 함

내장이 좋은 경우:

- rootfs mount 전에 필요한 장치
- 부팅 초기부터 필요한 저장장치/파일시스템/버스 드라이버
- 모듈 로딩 인프라가 없는 작은 시스템

### 23.2 Kbuild Makefile 문법

```makefile
obj-y += mem.o random.o
obj-$(CONFIG_TTY_PRINTK) += ttyprintk.o
```

의미:

| 값 | 의미 |
|---|---|
| `obj-y` | 커널 이미지에 내장 |
| `obj-m` | 모듈 `.ko`로 빌드 |
| `obj-n` 또는 미설정 | 빌드하지 않음 |
| `obj-$(CONFIG_FOO)` | Kconfig 값에 따라 `y/m/n` 결정 |

예:

```text
CONFIG_TTY_PRINTK=y -> ttyprintk.o 내장
CONFIG_TTY_PRINTK=m -> ttyprintk.ko 모듈
CONFIG_TTY_PRINTK=n -> 빌드 안 함
```

### 23.3 커널 이미지 종류

빌드 예:

```bash
make -j$(nproc) ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- Image
make -j$(nproc) ARCH=arm CROSS_COMPILE=arm-linux-gnueabihf- zImage
make -j$(nproc) ARCH=arm CROSS_COMPILE=arm-linux-gnueabihf- uImage
```

이미지:

| 이미지 | 의미 |
|---|---|
| `vmlinux` | ELF 형식 커널 바이너리. 디버깅 정보 포함 가능 |
| `Image` | 압축되지 않은 커널 이미지 |
| `zImage` | 압축된 커널 이미지 + 압축 해제 코드 |
| `uImage` | U-Boot legacy header가 붙은 이미지 |
| FIT image | U-Boot에서 커널/DTB/initramfs/서명 등을 묶는 현대적 포맷 |

[보충] ARM64에서는 보통 `Image`를 만들고 U-Boot에서 `booti`로 부팅한다. ARM 32-bit에서는 `zImage`와 `bootz`, 또는 `uImage`와 `bootm` 조합을 자주 본다.

### 23.4 커널 부팅 후 사용자 공간 시작

커널 초기화 후:

```text
init/main.c
  -> rest_init()
  -> kernel_init()
  -> rootfs 또는 initramfs 준비
  -> /init 실행 시도
  -> /sbin/init, /etc/init, /bin/init, /bin/sh 등 fallback
```

원문 내용:

- 커널 초기화 후 사용자 공간을 제공하기 위해 루트 파일시스템을 마운트한다.
- initramfs가 있으면 `/init`을 실행할 수 있다.
- initramfs가 없으면 커널 command line의 `root=` 인자가 지정한 블록 장치 파일시스템을 마운트한다.

### 23.5 커널 command line

커널 command line은 부트로더가 전달한다.

예:

```text
console=ttyAMA0,115200 root=/dev/mmcblk0p2 rootwait rw
```

자주 보는 인자:

| 인자 | 의미 |
|---|---|
| `console=` | 커널 콘솔 장치 |
| `root=` | 루트 파일시스템 장치 |
| `rootwait` | root 장치가 나타날 때까지 대기 |
| `rw`/`ro` | rootfs 읽기쓰기/읽기전용 마운트 |
| `init=` | PID 1로 실행할 프로그램 지정 |
| `loglevel=` | 커널 로그 레벨 |

### 23.6 BusyBox와 Toybox

BusyBox/Toybox:

- 여러 리눅스 기본 명령어를 하나의 바이너리로 묶어 제공
- 작은 임베디드 rootfs 구성에 유용
- `sh`, `ls`, `cp`, `mount`, `ifconfig` 등 기능 제공 가능

---

## 24. Yocto

### 24.1 Yocto가 해결하는 문제

Yocto는 임베디드 리눅스 배포판을 만들기 위한 빌드 프레임워크다.

생성 가능:

- cross toolchain
- bootloader
- Linux kernel
- root filesystem
- packages
- SDK
- 최종 이미지

핵심:

- 레시피(`.bb`)와 메타데이터를 기반으로 빌드한다.
- BitBake가 task scheduler 역할을 한다.
- 레이어를 추가해 기능과 BSP를 확장한다.

### 24.2 주요 구성 요소

| 구성 요소 | 역할 |
|---|---|
| OE-Core | OpenEmbedded와 공유하는 핵심 메타데이터 |
| BitBake | 레시피 task scheduler/executor |
| Poky | Yocto Project reference distribution |
| Documentation | 매뉴얼과 개발 가이드 |
| Toaster | BitBake/메타데이터 웹 UI |

레시피 파일:

```text
*.bb       -> recipe
*.bbappend -> 기존 recipe 확장
*.inc      -> 여러 recipe에서 include/require하는 공통 파일
```

### 24.3 레시피 예

```bitbake
require u-boot-common.inc
require u-boot.inc

DEPENDS += "bc-native dtc-native python3-pyelftools-native"

SRC_URI += "file://CVE-2024-57254.patch \
            file://CVE-2024-57255.patch \
"
```

의미:

| 항목 | 의미 |
|---|---|
| `require` | 다른 `.inc` 파일을 반드시 포함. 없으면 에러 |
| `DEPENDS` | 빌드 시 필요한 의존 recipe |
| `-native` | 빌드 호스트에서 실행되는 도구 |
| `SRC_URI` | 소스, 패치, 파일 위치 |
| `file://` | recipe 또는 layer의 files 디렉터리 등에서 가져오는 로컬 파일 |

### 24.4 BitBake target

| target | 의미 |
|---|---|
| `core-image-minimal` | 테스트용 소형 콘솔 기반 이미지 |
| `core-image-full-cmdline` | 표준 CLI 환경과 타깃 지원이 더 포함된 콘솔 이미지 |
| `core-image-sato` | Sato GUI 기반 이미지 |
| `core-image-weston` | Wayland/Weston 기반 그래픽 이미지 |
| `meta-toolchain` | cross toolchain/SDK 계열 |
| `meta-ide-support` | IDE 통합 개발 지원 도구 |

빌드:

```bash
bitbake core-image-minimal
```

### 24.5 빌드 결과 디렉터리

BitBake 완료 후 `tmp/` 아래에서 자주 보는 경로:

| 경로 | 의미 |
|---|---|
| `tmp/work/` | recipe별 빌드 작업 디렉터리 |
| `tmp/deploy/` | 배포용 최종 산출물 |
| `tmp/deploy/images/<machine>/` | 부트로더, 커널, rootfs 이미지 |
| `tmp/deploy/rpm/` | RPM 패키지 |
| `tmp/deploy/ipk/` | IPK 패키지 |
| `tmp/deploy/deb/` | DEB 패키지 |
| `tmp/deploy/licenses/` | 패키지별 라이선스 정보 |
| `tmp/sysroots-components/` | sysroot 구성요소 |

### 24.6 레이어

Yocto는 기본 레시피와 메타데이터에 레이어를 추가하는 방식으로 확장한다.

레이어에서 확인할 것:

- `conf/layer.conf`
- README
- 라이선스 파일
- 어떤 Yocto release와 호환되는지
- 의존하는 다른 레이어
- layer priority

자주 쓰는 명령:

```bash
bitbake-layers show-layers
bitbake-layers add-layer ../meta-my-layer
bitbake-layers show-recipes
```

시험 포인트:

- Yocto 자체가 배포판 이름이라기보다 커스텀 임베디드 리눅스 배포판을 만드는 프로젝트/프레임워크다.
- Poky는 레퍼런스 배포판이다.
- BitBake는 빌드 엔진이다.
- OpenEmbedded-Core는 공통 메타데이터다.
- `.bb`는 recipe다.
- layer 추가 시 의존성과 Yocto 버전 호환성을 확인해야 한다.

---

## 25. Yocto를 이용한 프로그램 개발

### 25.1 개발 흐름

Yocto에서 사용자 프로그램을 추가하는 일반 흐름:

```text
레이어 생성
  -> 소스 파일 작성
  -> 레시피(.bb) 작성
  -> 이미지에 추가
  -> bitbake로 빌드
```

### 25.2 레이어 생성 및 관리

`bitbake-layers` 명령:

| 명령 | 의미 |
|---|---|
| `bitbake-layers create-layer <name>` | 레이어 디렉터리 생성 |
| `bitbake-layers show-layers` | 현재 등록된 레이어 목록 |
| `bitbake-layers add-layer <path>` | 빌드에 레이어 추가 |
| `bitbake-layers remove-layer <path>` | 빌드에서 레이어 제거 |
| `bitbake-layers show-recipes` | 사용 가능한 레시피 목록 |

예:

```bash
bitbake-layers create-layer ../meta-test
bitbake-layers add-layer ../meta-test
bitbake-layers show-layers
```

### 25.3 레시피 작성 예

```bitbake
SUMMARY = "Simple helloworld application"
SECTION = "hello"
LICENSE = "MIT"

LIC_FILES_CHKSUM = "file://${COMMON_LICENSE_DIR}/GPL-2.0;md5=801f80980d171dd6425610833a22dbe6"

SRC_URI = "file://helloworld.c"

S = "${WORKDIR}/sources"
UNPACKDIR = "${S}"

do_compile() {
    ${CC} ${CFLAGS} ${LDFLAGS} ${S}/helloworld.c -o ${B}/helloworld
}

do_install() {
    install -d ${D}${bindir}
    install -m 0755 ${B}/helloworld ${D}${bindir}
}
```

주요 변수:

| 변수 | 의미 |
|---|---|
| `${S}` | 소스 디렉터리 |
| `${B}` | 빌드 산출물 디렉터리 |
| `${D}` | 설치 대상 디렉터리(staging) |
| `${CC}` | 크로스 컴파일러 |
| `${CFLAGS}` | 컴파일 플래그 |
| `${LDFLAGS}` | 링크 플래그 |
| `${bindir}` | 보통 `/usr/bin` |

### 25.4 이미지에 패키지 추가

`conf/local.conf`에서:

```conf
IMAGE_INSTALL:append = " helloworld"
```

[주의] 패키지 이름 앞에 공백 하나가 반드시 있어야 한다. `append`는 기존 값 뒤에 그대로 붙이므로 공백이 없으면 이전 패키지 이름과 합쳐진다.

### 25.5 BitBake 주요 태스크

| 태스크 | 의미 |
|---|---|
| `do_fetch` | 소스 코드 다운로드 |
| `do_unpack` | 소스 아카이브 압축 해제 |
| `do_patch` | 패치 파일 적용 |
| `do_configure` | configure 스크립트 실행 |
| `do_compile` | 컴파일 수행 |
| `do_install` | 빌드 결과를 rootfs staging에 설치 |
| `do_package` | 패키지 생성 |
| `do_rootfs` | rootfs 이미지 생성 |
| `do_image` | 최종 이미지 생성 |
| `do_populate_sdk` | SDK 생성 |

태스크 목록 확인:

```bash
bitbake -c listtasks core-image-minimal
```

### 25.6 메타데이터 파일 종류

| 확장자 | 의미 |
|---|---|
| `.bb` | 레시피. 빌드 정보(소스 위치, 의존성, 컴파일/설치 방법) 정의 |
| `.bbappend` | 기존 레시피를 확장하거나 세부 항목 덮어쓰기 |
| `.inc` | 여러 레시피에서 `include`/`require`로 포함하는 공통 정보 |
| `.bbclass` | 공통 빌드 정보. `inherit` 키워드로 상속 |
| `.conf` | 빌드 프로세스를 제어하는 구성 변수 |

[보충] `require`는 대상 파일이 없으면 에러가 발생하고, `include`는 없어도 무시한다.

---

## 26. 커널 디버깅 - ftrace

### 26.1 ftrace 개요

ftrace는 Linux 커널에 내장된 함수 단위 트레이싱 도구다.

특징:

- 커널에 부하를 거의 주지 않고 트레이싱 가능
- 실행 중인 커널을 실시간으로 디버깅 가능
- 성능 분석, 부팅 문제, 드라이버 디버깅에 유용

ftrace 인터페이스 위치:

```text
/sys/kernel/debug/tracing
```

선결 조건:

- ftrace 관련 코드가 포함된 리눅스 커널이 필요하다.
- debugfs가 마운트되어야 한다: `mount -t debugfs nodev /sys/kernel/debug`

커널 설정(`.config`):

```text
CONFIG_FUNCTION_TRACER=y
CONFIG_FUNCTION_GRAPH_TRACER=y
CONFIG_DYNAMIC_FTRACE=y
```

### 26.2 ftrace 주요 기능

| 기능 | 설명 |
|---|---|
| function tracer | 함수 호출 흐름 추적 |
| function_graph | 함수 호출 관계 + 실행 시간 표시 |
| irqsoff | IRQ 비활성화 시간 추적 |
| sched_switch | 스케줄링 이벤트 추적 |
| mmiotrace | 메모리맵 I/O 분석 |
| wakeup/wakeup_rt | wakeup latency 분석 |

### 26.3 주요 파일

| 파일 | 의미 |
|---|---|
| `trace` | ftrace 로그가 저장되는 파일 |
| `tracing_on` | ftrace on/off (`1`/`0`) |
| `available_tracers` | 사용 가능한 tracer 종류 |
| `current_tracer` | 현재 설정된 tracer. 기본값 `nop` |
| `set_ftrace_filter` | 트레이스할 함수 지정 |
| `set_ftrace_notrace` | 트레이스에서 제외할 함수 지정 |
| `available_filter_functions` | 트레이싱 가능한 함수 목록 |
| `set_ftrace_pid` | 특정 프로세스/스레드 ID만 추적 |
| `buffer_size_kb` | 로그 버퍼 크기 |
| `function_profile_enable` | 함수 프로파일링 on/off |

[주의] `current_tracer`를 `function` 또는 `function_graph`로 설정할 때 `set_ftrace_filter`에 함수를 지정하지 않으면 `available_filter_functions`에 있는 모든 함수를 트레이스하므로 시스템이 멈출 수 있다.

### 26.4 기본 사용법

```bash
# 추적 끄기
echo 0 > /sys/kernel/debug/tracing/tracing_on

# tracer 설정
echo function > /sys/kernel/debug/tracing/current_tracer

# 추적할 함수 지정
echo do_sys_open > /sys/kernel/debug/tracing/set_ftrace_filter

# 추적 시작
echo 1 > /sys/kernel/debug/tracing/tracing_on

# 어떤 동작 수행 후 결과 확인
cat /sys/kernel/debug/tracing/trace
```

trace 출력 형식:

```text
# tracer: function
#
#           TASK-PID     CPU#  |||||  TIMESTAMP  FUNCTION
#              | |         |   |||||     |         |
```

### 26.5 function_graph tracer

function_graph는 ftrace의 고급 모드다.

제공 정보:

| 항목 | 설명 |
|---|---|
| 함수 호출 순서 | 어떤 함수가 어떤 함수를 호출했는지 |
| 중첩 깊이 | 함수 호출 스택 구조 |
| 실행 시간 | 각 함수의 entry ~ exit까지 걸린 시간 |
| 스케줄링 지연 | 함수 실행 중 스케줄링 발생 여부 |

특정 함수만 추적:

```bash
echo vfs_open > /sys/kernel/debug/tracing/set_graph_function
echo function_graph > /sys/kernel/debug/tracing/current_tracer
echo 1 > /sys/kernel/debug/tracing/tracing_on
cat /etc/hostname > /dev/null
cat /sys/kernel/debug/tracing/trace
```

실행 시간 기준 필터링:

```bash
echo 10 > /sys/kernel/debug/tracing/tracing_thresh
```

- 실행 시간이 10µs 이상 걸린 함수만 trace에 남긴다.

### 26.6 이벤트 트레이싱

이벤트 단위로 커널 동작을 추적할 수 있다.

주요 이벤트:

| 서브시스템 | 이벤트 | 의미 |
|---|---|---|
| `irq/` | `irq_handler_entry` | 인터럽트 발생 시작. 번호/이름 출력 |
| `irq/` | `irq_handler_exit` | 인터럽트 핸들링 완료 |
| `irq/` | `softirq_raise` | Soft IRQ 서비스 실행 요청 |
| `irq/` | `softirq_entry` | Soft IRQ 서비스 시작 |
| `irq/` | `softirq_exit` | Soft IRQ 서비스 완료 |
| `sched/` | `sched_switch` | 문맥 교환 동작 |
| `sched/` | `sched_wakeup` | 프로세스 깨우기 동작 |

이벤트 트레이싱 스크립트 예:

```bash
#!/bin/bash

echo 0 > /sys/kernel/debug/tracing/tracing_on
sleep 1

echo nop > /sys/kernel/debug/tracing/current_tracer
echo 0 > /sys/kernel/debug/tracing/events/enable
sleep 1

echo 8096 > /sys/kernel/debug/tracing/buffer_size_kb

echo 1 > /sys/kernel/debug/tracing/events/sched/sched_switch/enable
echo 1 > /sys/kernel/debug/tracing/events/sched/sched_wakeup/enable
sleep 1

echo 1 > /sys/kernel/debug/tracing/events/irq/irq_handler_entry/enable
echo 1 > /sys/kernel/debug/tracing/events/irq/irq_handler_exit/enable

echo 1 > /sys/kernel/debug/tracing/tracing_on
```

로그 수집 스크립트 예:

```bash
#!/bin/bash

echo 0 > /sys/kernel/debug/tracing/tracing_on
echo "ftrace off"
sleep 3

cp /sys/kernel/debug/tracing/trace .
mv trace ftrace_log.txt
```

시험 포인트:

- ftrace는 커널 내장 트레이싱 도구로 별도 설치가 필요 없다.
- debugfs 마운트와 커널 CONFIG 설정이 선결 조건이다.
- `function` tracer는 함수 호출만 기록하고, `function_graph` tracer는 호출 관계와 실행 시간까지 표시한다.
- `set_ftrace_filter`를 지정하지 않고 `function` tracer를 활성화하면 모든 함수를 추적해 시스템이 느려지거나 멈출 수 있다.
- 이벤트 트레이싱은 특정 서브시스템(IRQ, 스케줄러 등)의 동작을 세밀하게 추적할 수 있다.

---

## 27. 시험 대비 예상 문답

### Q1. 커널 공간과 사용자 공간을 나누는 이유는?

안정성과 보안을 위해서다. 사용자 프로그램이 직접 하드웨어나 커널 메모리에 접근하면 시스템 전체를 망가뜨릴 수 있으므로, 커널이 특권 모드에서 자원을 관리하고 사용자 프로그램은 시스템 호출을 통해 요청한다.

### Q2. `.ko` 파일은 무엇인가?

Loadable Kernel Module 파일이다. 실행 중인 커널에 동적으로 로드되어 커널 공간에서 실행되는 객체 파일이다.

### Q3. `insmod`와 `modprobe` 차이는?

`insmod`는 지정한 `.ko`를 직접 로드하며 의존성을 자동 처리하지 않는다. `modprobe`는 `/lib/modules/$(uname -r)`의 의존성 정보를 이용해 필요한 모듈을 함께 로드한다.

### Q4. 캐릭터 디바이스 등록 순서는?

`alloc_chrdev_region`으로 번호를 받고, `cdev_init`으로 `cdev`와 `file_operations`를 연결하고, `cdev_add`로 커널에 등록한다. `/dev` 자동 생성을 위해 `class_create`, `device_create`를 사용한다.

### Q5. 사용자 버퍼를 커널에서 직접 역참조하면 안 되는 이유는?

사용자 주소는 커널 주소가 아니며 유효하지 않거나 페이지 폴트가 날 수 있다. 보안과 안정성을 위해 `copy_from_user`, `copy_to_user`, `get_user`, `put_user`를 사용해야 한다.

### Q6. `read()`와 `write()`에서 반환해야 하는 값은?

성공 시 처리한 바이트 수를 반환한다. 실패 시 `-EFAULT`, `-EAGAIN`, `-EINVAL` 같은 음수 errno를 반환한다.

### Q7. `ioctl`의 `_IOR`, `_IOW` 방향은 누구 기준인가?

사용자 공간 기준이다. `_IOR`은 사용자가 드라이버에서 읽는 명령이라 커널은 보통 `copy_to_user`를 한다. `_IOW`는 사용자가 드라이버로 쓰는 명령이라 커널은 보통 `copy_from_user`를 한다.

### Q8. Raspberry Pi 4 GPIO의 `/dev/mem` 주소는 왜 `0x7E200000`이 아니라 `0xFE200000`인가?

`0x7E200000`은 BCM2711 문서의 legacy peripheral 주소다. Low Peripheral mode에서 ARM physical address는 `0xFE000000 + 0x00200000 = 0xFE200000`으로 보이므로 CPU가 접근하는 물리 주소를 사용해야 한다.

### Q9. GPIO 18을 output으로 설정할 때 어느 비트를 만지는가?

GPIO 한 핀은 FSEL 3비트를 사용한다. GPIO 18은 `18 / 10 = 1`이므로 `GPFSEL1`, `18 % 10 = 8`이므로 bit `26:24`를 설정한다.

### Q10. 임베디드 리눅스의 필수 구성 요소는?

툴체인, 부트로더, 커널, 루트 파일시스템, 애플리케이션이다.

### Q11. 부트로더의 핵심 역할은?

하드웨어 초기화, DRAM 설정, 커널/DTB/initramfs 로드, bootargs 설정, 커널 entry point로 제어권 전달이다.

### Q12. Device Tree는 무엇인가?

하드웨어 구성을 커널에 전달하는 정적 데이터 구조다. `.dts` 소스를 컴파일해 `.dtb` 바이너리로 만들며, 보통 부트로더가 커널에 전달한다.

### Q13. `compatible` 프로퍼티의 역할은?

커널이 해당 장치에 맞는 드라이버를 찾을 때 사용하는 문자열이다. 보통 `"vendor,device"` 형식을 사용한다.

### Q14. `obj-y`, `obj-m` 차이는?

`obj-y`는 커널 이미지에 내장 빌드하고, `obj-m`은 로드 가능한 모듈 `.ko`로 빌드한다.

### Q15. `vmlinux`, `Image`, `zImage`, `uImage` 차이는?

`vmlinux`는 ELF 커널 바이너리다. `Image`는 압축되지 않은 커널 이미지, `zImage`는 압축된 커널 이미지, `uImage`는 U-Boot legacy header가 붙은 이미지다.

### Q16. Yocto에서 BitBake의 역할은?

레시피와 메타데이터를 해석하고 태스크 의존성을 계산해 소스 다운로드, 패치, 설정, 컴파일, 패키징, 이미지 생성을 수행하는 빌드 엔진이다.

### Q17. Yocto의 `.bb` 파일은 무엇인가?

레시피 파일이다. 어떤 소스를 어디서 가져오고, 어떤 의존성이 있으며, 어떻게 configure/compile/install/package할지 정의한다.

### Q18. `core-image-minimal`은 무엇인가?

테스트와 커스텀 이미지의 기초로 쓰기 좋은 작은 콘솔 기반 root filesystem 이미지다.

---

### Q19. ftrace는 무엇이고 어떻게 사용하는가?

ftrace는 Linux 커널에 내장된 함수 단위 트레이싱 도구다. `/sys/kernel/debug/tracing` 경로의 파일들을 `echo`/`cat`으로 조작하여 tracer를 설정하고 결과를 확인한다. `function` tracer는 함수 호출을 기록하고, `function_graph` tracer는 호출 관계와 실행 시간까지 보여준다.

### Q20. ftrace에서 `set_ftrace_filter`를 지정하지 않으면 어떻게 되는가?

`available_filter_functions`에 있는 모든 함수가 추적 대상이 되어 시스템이 극도로 느려지거나 멈출 수 있다. 반드시 추적할 함수를 지정해야 한다.

### Q21. Yocto에서 사용자 프로그램을 이미지에 추가하는 절차는?

레이어 생성 → 소스 파일 작성 → 레시피(`.bb`) 작성 → `conf/local.conf`에 `IMAGE_INSTALL:append = " <패키지명>"` 추가 → `bitbake <이미지명>`으로 빌드한다.

### Q22. BitBake의 주요 태스크 순서는?

`do_fetch` → `do_unpack` → `do_patch` → `do_configure` → `do_compile` → `do_install` → `do_package` → `do_rootfs` → `do_image` 순이다.

### Q23. Yocto 레시피에서 `require`와 `include`의 차이는?

`require`는 대상 파일이 없으면 에러가 발생하고, `include`는 없어도 무시하고 넘어간다.

---

## 28. 암기용 표

### 커널 모듈

| 키워드 | 답 |
|---|---|
| 초기화 등록 | `module_init()` |
| 종료 등록 | `module_exit()` |
| 로그 | `printk()`, `pr_info()` |
| 로드 | `insmod`, `modprobe` |
| 언로드 | `rmmod`, `modprobe -r` |
| 의존성 갱신 | `depmod -a` |

### 캐릭터 디바이스

| 키워드 | 답 |
|---|---|
| 번호 할당 | `alloc_chrdev_region()` |
| cdev 초기화 | `cdev_init()` |
| cdev 등록 | `cdev_add()` |
| cdev 삭제 | `cdev_del()` |
| 클래스 생성 | `class_create()` |
| 장치 생성 | `device_create()` |
| 장치 삭제 | `device_destroy()` |
| 번호 반환 | `unregister_chrdev_region()` |

### 사용자/커널 복사

| 방향 | 함수 |
|---|---|
| user -> kernel | `copy_from_user()` |
| kernel -> user | `copy_to_user()` |
| user 값 1개 읽기 | `get_user()` |
| user 값 1개 쓰기 | `put_user()` |

### ioctl

| 매크로 | 의미 |
|---|---|
| `_IO` | 데이터 없음 |
| `_IOR` | user가 read |
| `_IOW` | user가 write |
| `_IOWR` | 양방향 |
| `_IOC_TYPE` | magic |
| `_IOC_NR` | number |
| `_IOC_SIZE` | size |
| `_IOC_DIR` | direction |

### 임베디드 리눅스

| 키워드 | 답 |
|---|---|
| 툴체인 | 타깃용 컴파일러/링커/라이브러리 |
| 부트로더 | 보드 초기화, 커널 로드 |
| 커널 | 자원 관리, HW 제어 |
| rootfs | 사용자 공간 프로그램/라이브러리 |
| Device Tree | 하드웨어 설명 데이터 |
| Yocto | 커스텀 임베디드 리눅스 빌드 프레임워크 |

### ftrace

| 키워드 | 답 |
|---|---|
| 인터페이스 위치 | `/sys/kernel/debug/tracing` |
| 기본 tracer | `nop` |
| 함수 호출 추적 | `function` tracer |
| 호출 관계 + 시간 | `function_graph` tracer |
| 추적 on/off | `tracing_on` 파일에 `1`/`0` |
| 추적 함수 지정 | `set_ftrace_filter` |
| 로그 확인 | `cat trace` |
| 선결 조건 | debugfs 마운트, `CONFIG_FUNCTION_TRACER=y` |

### Yocto 개발

| 키워드 | 답 |
|---|---|
| 레이어 생성 | `bitbake-layers create-layer` |
| 레이어 추가 | `bitbake-layers add-layer` |
| 태스크 목록 | `bitbake -c listtasks` |
| 이미지에 패키지 추가 | `IMAGE_INSTALL:append = " <name>"` |
| 레시피 확장 | `.bbappend` |
| 공통 포함 | `.inc` + `require`/`include` |
| 빌드 실행 | `bitbake <이미지명>` |

---

## 29. 원본 이미지 참조 목록

이미지는 원문 Obsidian 링크 기준이다. 정리본에서는 핵심 내용을 텍스트화했지만, 도식 형태가 필요하면 아래 원본 이미지를 함께 보면 된다.

### Week 12 Day50

- `Pasted image 20260608091907.png`: 커널 역할/하드웨어 인터페이스 흐름
- `Pasted image 20260608092622.png`: 커널 구조 비교
- `Pasted image 20260608094933.png`: 사용자 영역/커널 영역
- `Pasted image 20260608094940.png`: 주소 공간과 커널 접근
- `Pasted image 20260608095359.png`: 디바이스 드라이버 추상화
- `Pasted image 20260608102835.png`: 커널 소스 디렉터리
- `Pasted image 20260608104940.png`: 프로세스 기술자/태스크 구조체
- `Pasted image 20260608105020.png`: 프로세스 상태 천이
- `Pasted image 20260608105041.png`: `fork()` 흐름
- `Pasted image 20260608105702.png`: 문맥 교환
- `Pasted image 20260608105827.png`: 메모리 주소/페이지
- `Pasted image 20260608105832.png`: 메모리 관리 도식
- `Pasted image 20260608105942.png`: 파일 시스템/VFS
- `Pasted image 20260608110301.png`: 커널과 디바이스 통신
- `Pasted image 20260608144408.png`: 모듈 빌드/로드 예
- `Pasted image 20260608151321.png`: 드라이버 관점 리눅스 구조
- `Pasted image 20260608151335.png`: 응용프로그램 관점 리눅스 구조
- `Pasted image 20260608153441.png`: 캐릭터 디바이스 구조
- `Pasted image 20260608154537.png`: 드라이버 로드/언로드
- `Pasted image 20260608155454.png`: `file_operations`
- `Pasted image 20260608162637.png`: 캐릭터 디바이스 등록 흐름
- `Pasted image 20260608162642.png`: 장치 등록/해제 흐름

### Week 12 Day51

- `Pasted image 20260609104412.png`: 장치 파일/dentry/inode 구조
- `Pasted image 20260609104422.png`: dentry 연결 구조
- `Pasted image 20260609112813.png`: 장치 파일 권한
- `Pasted image 20260609115604.png`: open 인자 처리 흐름
- `Pasted image 20260609115948.png`: `struct file`
- `Pasted image 20260609121640.png`: read/write 구현 고려사항
- `Pasted image 20260609134230.png`: write 흐름
- `Pasted image 20260609134838.png`: read 흐름
- `Pasted image 20260609150547.png`: ioctl cmd 비트 필드
- `Pasted image 20260609150756.png`: 사용자 앱과 ioctl 명령 관계
- `Pasted image 20260610092805.png`: Raspberry Pi GPIO register 직접 제어
- `Pasted image 20260610093933.png`: BCM2711 주소 변환
- `Pasted image 20260610093951.png`: Low Peripheral mode 주소 설명

### Week 12 Day51 BCM2711

- `Pasted image 20260610095117.png`: ARM virtual/physical/peripheral address 관계
- `Pasted image 20260610112814.png`: GPIO register block
- `Pasted image 20260610112941.png`: GPIO register view
- `Pasted image 20260610112950.png`: GPIO register table
- `Pasted image 20260610112957.png`: GPFSEL 설명
- `Pasted image 20260610113427.png`: GPIO function table
- `Pasted image 20260610113439.png`: GPIO alternate function table
- `Pasted image 20260610113451.png`: GPIO alternate function table

### Week 12 Day52

- `Pasted image 20260610121922.png`: 커널 GPIO API 사용 시 GPIO 번호 확인

### Week 12 Day53

- `Pasted image 20260611115844.png`: 부팅 시 모듈 자동 적재 설정
- `Pasted image 20260611141020.png`: 시스템 호출 번호/테이블

### Week 13 Day54

- `Pasted image 20260615113920.png`: 부트 순서
- `Pasted image 20260615115701.png`: 장치 트리 계층 구조
- `Pasted image 20260615121757.png`: DTS/DTB 예시

### Week 13 Day55

- `Pasted image 20260616104332.png`: U-Boot 구조/흐름
- `Pasted image 20260616160439.png`: 사용자 공간/커널 공간
- `Pasted image 20260616160507.png`: 커널 공간 관련 도식
- `Pasted image 20260616162102.png`: 커널 이미지/빌드 결과
- `Pasted image 20260616162243.png`: 커널 이미지 관계
- `Pasted image 20260616162502.png`: 커널 command line
- `Pasted image 20260616163050.png`: rootfs/init 관련 흐름

### Week 13 Day56

- 원문에 별도 이미지 없음

### Week 13 Day58

- `Pasted image 20260619091840.png`: Yocto/Poky 구성 요소
- `Pasted image 20260619091851.png`: Yocto 구성 요소 상세
- `Pasted image 20260619092943.png`: BitBake 메타데이터 구조
- `Pasted image 20260619094446.png`: Yocto 프로그램 개발 흐름
- `Pasted image 20260619095258.png` ~ `Pasted image 20260619095326.png`: 빌드 결과물/이미지 생성
- `Pasted image 20260619101853.png`: 레이어 디렉터리 구조
- `Pasted image 20260619103000.png`: 이미지에 패키지 추가
- `Pasted image 20260619141346.png`: ftrace 주요 기능
- `Pasted image 20260619141658.png`: ftrace 커널 설정 확인
- `Pasted image 20260619144032.png` ~ `Pasted image 20260619144611.png`: ftrace 실습 과정
- `Pasted image 20260619152039.png`: kfree/kmalloc 트레이싱
- `Pasted image 20260619152503.png` ~ `Pasted image 20260619154512.png`: 이벤트 트레이싱 실습
- `Pasted image 20260619160811.png`: 모듈 적재 디버깅
- `Pasted image 20260619163033.png`: 모듈 디버깅 결과

---

## 30. 마지막 체크리스트

- 커널 모듈에서 `module_init`/`module_exit` 구분 가능
- `insmod`/`modprobe` 차이 설명 가능
- 사용자 공간과 커널 공간 차이 설명 가능
- `copy_from_user`/`copy_to_user` 방향 암기
- 캐릭터 디바이스 등록/해제 순서 암기
- `file_operations` 멤버와 사용자 시스템 호출 연결 가능
- `ioctl` cmd 작성/해석 매크로 암기
- BCM2711 GPIO 주소 `0x7E200000`과 `0xFE200000` 차이 설명 가능
- GPIO 18의 FSEL bit 계산 가능
- 임베디드 리눅스 구성 요소 5개 설명 가능
- 툴체인 native/cross 차이 설명 가능
- Boot ROM/SPL/U-Boot/kernel/rootfs 부트 흐름 설명 가능
- Device Tree의 `compatible`, `reg`, `interrupts` 설명 가능
- `obj-y`/`obj-m`/`CONFIG_` 관계 설명 가능
- Yocto, BitBake, Poky, OE-Core, recipe, layer 관계 설명 가능
- Yocto에서 레이어 생성 → 레시피 작성 → 이미지 추가 → 빌드 흐름 설명 가능
- BitBake 주요 태스크 순서(`do_fetch` ~ `do_image`) 설명 가능
- `.bb`, `.bbappend`, `.inc`, `.bbclass`, `.conf` 파일 역할 구분 가능
- ftrace 인터페이스 위치와 선결 조건 설명 가능
- `function`과 `function_graph` tracer 차이 설명 가능
- `set_ftrace_filter` 미지정 시 위험성 설명 가능
- 이벤트 트레이싱으로 IRQ/스케줄링 동작 추적 방법 설명 가능
