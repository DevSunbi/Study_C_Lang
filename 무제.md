좋다. 아래는 **파이오링크 L2~L7 장비 개발직 기준으로 네가 부족하다고 한 개념들 전체 설명**이다.  
책처럼 깊게 파면 끝이 없으니까, **면접/자기소개서/포트폴리오에서 써먹을 수 있는 수준**으로 정리한다.

---

# 0. 큰 그림: L2~L7이 뭔가

네트워크는 데이터를 여러 계층으로 나눠 처리한다. 파이오링크 직무에서 말하는 L2/L3/L4/L7은 보통 OSI 계층 기준 표현이다.

|계층|이름|핵심 단위|대표 개념|장비/기능|
|---|---|---|---|---|
|L2|데이터링크|Ethernet Frame|MAC, Switch, VLAN, STP|L2 스위치|
|L3|네트워크|IP Packet|IP, ARP, Routing, ICMP|라우터, L3 스위치|
|L4|전송|TCP/UDP Segment|Port, Session, TCP 연결|L4 로드밸런서|
|L7|응용|HTTP Message|URL, Header, Host, Cookie|L7 로드밸런서, WAF|

네가 지금 쓴 자기소개서 문장에서는 “L2/L3 스위칭과 L4/L7 패킷 처리”라고 했는데, 이건 **스위치가 MAC/IP/Port/HTTP 정보를 보고 패킷 또는 요청을 어떻게 처리하는지 이해하겠다**는 뜻이다.

---

# 1. Ethernet

Ethernet은 LAN에서 가장 널리 쓰이는 L2 통신 방식이다. L2에서는 IP 주소가 아니라 **MAC 주소**를 기준으로 통신한다. Linux bridge 문서도 bridge의 목적을 “destination MAC 주소를 기준으로 frame을 filter/forward하는 것”이라고 설명한다.

## 핵심 개념

### MAC Address

네트워크 인터페이스 카드에 붙는 L2 주소다.

예:

```
00:1A:2B:3C:4D:5E
```

IP 주소가 “논리적 주소”라면, MAC 주소는 같은 LAN 안에서 실제 프레임을 전달할 때 쓰는 주소다.

### Ethernet Frame

L2에서 주고받는 데이터 단위다.

대략 구조는 이렇게 보면 된다.

```
[Dst MAC][Src MAC][EtherType][Payload][FCS]
```

- Dst MAC: 목적지 MAC 주소
- Src MAC: 출발지 MAC 주소
- EtherType: 안에 들어있는 상위 프로토콜 종류. 예: IPv4, ARP
- Payload: 실제 데이터
- FCS: 오류 검출용 값

### MTU

MTU는 한 번에 실을 수 있는 L3 payload 크기다. 일반 Ethernet에서는 보통 1500 bytes를 기준으로 많이 설명한다. MTU보다 큰 IP 패킷은 쪼개지거나, 경로에 따라 전송 실패가 날 수 있다.

### FCS

Frame Check Sequence다. 프레임이 전송 중 깨졌는지 확인하는 오류 검출 값이다.

## 면접식 답변

> Ethernet은 같은 LAN 안에서 MAC 주소를 기준으로 frame을 전달하는 L2 기술입니다. 스위치는 목적지 MAC 주소를 보고 어느 포트로 frame을 보낼지 판단합니다.

---

# 2. Switching

Switching은 L2 스위치가 Ethernet frame을 적절한 포트로 보내는 동작이다.

## MAC Address Table / FDB

스위치는 내부에 MAC 주소 테이블을 가진다. Linux에서는 bridge의 forwarding database, 즉 FDB로 볼 수 있고, `bridge fdb` 명령은 link에 알려진 Ethernet 주소 항목을 표시하거나 추가/삭제하는 데 사용된다.

예:

```
MAC 주소              포트AA:AA:AA:AA:AA:AA     port1BB:BB:BB:BB:BB:BB     port2
```

스위치는 이 표를 보고 “이 MAC은 어느 포트 뒤에 있다”를 판단한다.

## Learning

스위치는 들어온 프레임의 **출발지 MAC 주소**를 보고 학습한다.

예:

```
port1에서 Src MAC = AA:AA:AA:AA:AA:AA 프레임 수신→ 스위치가 "AA는 port1 뒤에 있구나" 저장
```

## Forwarding

목적지 MAC 주소가 MAC table에 있으면 해당 포트로만 보낸다.

```
Dst MAC = BBMAC table에 BB → port2→ port2로만 전송
```

## Flooding

목적지 MAC 주소를 모르면 모든 포트로 뿌린다. 단, 프레임이 들어온 포트로는 다시 보내지 않는다.

```
Dst MAC 모름→ 나머지 모든 포트로 전송
```

Linux bridge 문서에도 learning이 꺼져 있으면 FDB entry가 없는 트래픽을 flooding하게 된다고 설명되어 있다.

## Broadcast

목적지 MAC이 전체 대상인 경우다.

```
FF:FF:FF:FF:FF:FF
```

ARP Request가 대표적이다.

## 면접식 답변

> L2 스위치는 출발지 MAC을 보고 MAC table을 학습하고, 목적지 MAC이 table에 있으면 해당 포트로 forwarding합니다. 목적지 MAC을 모르면 flooding하고, broadcast frame은 같은 broadcast domain 안의 포트로 전달됩니다.

---

# 3. VLAN

VLAN은 하나의 물리 스위치를 논리적으로 여러 네트워크처럼 나누는 기술이다. IEEE 802.1Q는 Ethernet frame에 VLAN tag를 넣는 방식을 정의하며, Juniper 문서도 802.1Q VLAN이 Ethernet interface를 여러 logical interface로 나누는 방식이라고 설명한다.

## 왜 필요한가

스위치 하나에 여러 부서가 연결되어 있다고 하자.

```
개발팀 VLAN 10인사팀 VLAN 20보안팀 VLAN 30
```

같은 스위치에 꽂혀 있어도 VLAN이 다르면 기본적으로 서로 L2 통신이 안 된다.

## Access Port

하나의 VLAN에만 속하는 포트다. PC나 프린터 같은 단말이 연결된다.

```
port1 = VLAN 10 access
```

PC는 VLAN tag를 모른다. 스위치가 내부적으로 VLAN 10으로 취급한다.

## Trunk Port

여러 VLAN 트래픽을 한 포트로 보내는 방식이다. 보통 스위치-스위치 연결에 쓴다.

```
Switch A ---- trunk ---- Switch BVLAN 10, 20, 30 모두 전달
```

이때 frame에 VLAN tag가 붙는다.

## 802.1Q Tag

Ethernet frame 안에 VLAN ID 정보를 추가한다.

```
[Dst MAC][Src MAC][802.1Q Tag][EtherType][Payload][FCS]
```

## Native VLAN

Trunk에서 tag 없이 흐르는 VLAN이다. 설정이 어긋나면 보안 문제나 통신 장애가 생길 수 있다.

## 면접식 답변

> VLAN은 하나의 물리 네트워크를 여러 개의 논리적 L2 네트워크로 분리하는 기술입니다. Access port는 하나의 VLAN에 속한 단말용 포트이고, trunk port는 여러 VLAN을 태그 기반으로 전달하는 스위치 간 연결에 사용됩니다.

---

# 4. STP / RSTP

STP는 Spanning Tree Protocol이다. 스위치가 여러 경로로 연결될 때 L2 loop가 생기지 않게 막는 기술이다. Cisco 문서는 STP가 여러 경로로 연결된 switch/bridge 사이에서 loop 형성을 방지하며 BPDU 메시지를 교환해 loop를 감지하고 선택된 포트를 차단한다고 설명한다.

## 왜 필요한가

스위치가 이렇게 연결되면 loop가 생긴다.

```
Switch A  |    \  |     \Switch B -- Switch C
```

L2에는 TTL이 없다. 즉, frame이 loop에 빠지면 계속 돈다. 그 결과 broadcast storm이 발생할 수 있다.

## BPDU

Bridge Protocol Data Unit이다. 스위치들이 서로 “누가 root bridge인지”, “어떤 경로가 더 좋은지” 판단하기 위해 주고받는 제어 메시지다.

## Root Bridge

STP 트리의 기준이 되는 스위치다. 모든 스위치는 root bridge까지 가는 최적 경로를 계산한다.

## Port 상태

대표적으로 다음 상태가 있다.

```
Forwarding: 프레임 전달Blocking: loop 방지를 위해 차단
```

## RSTP

Rapid Spanning Tree Protocol이다. STP보다 빠르게 수렴하도록 개선된 방식이다.

## 면접식 답변

> STP는 L2 loop를 방지하기 위해 스위치 간 BPDU를 교환하고, root bridge를 기준으로 일부 포트를 blocking 상태로 두는 프로토콜입니다. RSTP는 STP보다 장애 발생 후 경로 전환이 빠른 개선 버전입니다.

---

# 5. ARP

ARP는 IP 주소를 MAC 주소로 바꾸기 위한 프로토콜이다. RFC 826은 ARP의 목적을 IP 같은 protocol address를 Ethernet address 같은 local network address로 변환하는 방법이라고 설명한다.

## 왜 필요한가

PC가 `192.168.0.10`에게 보내고 싶다고 해도, L2에서는 MAC 주소가 필요하다.

```
내가 아는 것: 192.168.0.10필요한 것: 192.168.0.10의 MAC 주소
```

그래서 ARP Request를 broadcast한다.

```
Who has 192.168.0.10?Tell 192.168.0.5
```

해당 IP를 가진 장비가 ARP Reply를 보낸다.

```
192.168.0.10 is at AA:BB:CC:DD:EE:FF
```

## ARP Cache

한 번 알게 된 IP-MAC 매핑은 잠시 저장된다.

```
arp -aip neigh
```

## Gateway와 ARP

다른 네트워크로 갈 때는 목적지 서버의 MAC을 찾는 게 아니라 **gateway의 MAC 주소**를 찾는다.

예:

```
내 PC: 192.168.0.5목적지: 8.8.8.8gateway: 192.168.0.1
```

이때 ARP로 찾는 것은 `8.8.8.8`의 MAC이 아니라 `192.168.0.1`의 MAC이다.

## 면접식 답변

> ARP는 같은 L2 네트워크 안에서 IP 주소에 대응하는 MAC 주소를 찾기 위한 프로토콜입니다. 같은 서브넷이면 목적지 IP의 MAC을 찾고, 다른 서브넷이면 gateway의 MAC 주소를 ARP로 찾습니다.

---

# 6. IPv4, Subnet, Routing

IP는 L3에서 출발지와 목적지를 식별하고 네트워크 간 전달을 가능하게 하는 프로토콜이다. RFC 791은 IP가 packet-switched 네트워크에서 source와 destination을 fixed length address로 식별하고 datagram을 전달하도록 설계되었다고 설명한다.

## IP Address

예:

```
192.168.0.10
```

네트워크 상의 논리 주소다.

## Subnet Mask / CIDR

어디까지가 네트워크 주소이고 어디까지가 호스트 주소인지 나눈다.

```
192.168.0.10/24
```

이 뜻은:

```
네트워크: 192.168.0.0호스트 범위: 192.168.0.1 ~ 192.168.0.254브로드캐스트: 192.168.0.255
```

## Gateway

다른 네트워크로 나갈 때 거치는 장비다.

```
192.168.0.10 → 8.8.8.8
```

이 경우 PC는 직접 8.8.8.8로 보내는 게 아니라 gateway로 보낸다.

## Routing Table

패킷을 어느 인터페이스/게이트웨이로 보낼지 정하는 표다. `ip route`는 Linux에서 kernel routing table의 route entry를 다루는 명령이다.

예:

```
ip route
```

출력 예:

```
default via 192.168.0.1 dev eth0192.168.0.0/24 dev eth0
```

의미:

```
192.168.0.0/24는 eth0로 직접 전달나머지는 192.168.0.1 gateway로 전달
```

## 면접식 답변

> IP는 네트워크 간 패킷 전달을 위한 L3 주소 체계입니다. 같은 서브넷이면 ARP로 목적지 MAC을 찾아 직접 보내고, 다른 서브넷이면 routing table을 보고 gateway로 전달합니다.

---

# 7. ICMP, Ping, Traceroute

ICMP는 네트워크 상태나 오류를 알려주는 L3 제어성 프로토콜이다. RFC 792는 ICMP가 gateway나 destination host가 source host와 통신하기 위해 사용하는 메시지라고 설명한다.

## Ping

Ping은 ICMP Echo Request/Reply를 사용한다.

```
ping 8.8.8.8
```

확인 가능한 것:

```
상대가 응답하는지왕복 지연시간 RTT패킷 손실률
```

## Traceroute

목적지까지 가는 경로를 확인한다.

원리 간단 설명:

```
TTL을 1부터 증가시키면서 중간 라우터의 ICMP 응답을 확인
```

## 면접식 답변

> ICMP는 네트워크 오류나 상태를 전달하는 제어용 프로토콜입니다. ping은 ICMP Echo로 도달성과 RTT를 확인하고, traceroute는 TTL을 조정해 목적지까지의 경로를 추적합니다.

---

# 8. TCP

TCP는 L4 전송 계층 프로토콜이다. RFC 9293은 TCP를 인터넷 프로토콜 스택의 중요한 transport-layer protocol로 설명한다.

## TCP의 특징

```
연결 지향신뢰성 보장순서 보장흐름 제어혼잡 제어재전송
```

## 3-Way Handshake

TCP 연결 시작 과정이다.

```
Client → Server : SYNServer → Client : SYN + ACKClient → Server : ACK
```

이후 데이터 송수신이 가능하다.

## 4-Way Close

TCP 연결 종료 과정이다.

```
FINACKFINACK
```

## Port

하나의 IP 안에서 어떤 프로세스와 통신할지 구분하는 값이다.

예:

```
HTTP: 80HTTPS: 443SSH: 22
```

## TCP 상태

자주 보는 상태:

```
LISTENSYN_SENTSYN_RECVESTABLISHEDFIN_WAITTIME_WAITCLOSE_WAIT
```

면접에서 `TIME_WAIT`나 `CLOSE_WAIT`를 물어볼 수 있다.

### TIME_WAIT

내가 먼저 정상 종료한 쪽에서 일정 시간 남는 상태다. 지연 패킷이 뒤늦게 도착하는 상황 등을 처리하기 위해 존재한다.

### CLOSE_WAIT

상대가 FIN을 보냈는데 내 애플리케이션이 아직 socket을 닫지 않은 상태다. CLOSE_WAIT이 많이 쌓이면 코드에서 close 처리가 안 되고 있을 가능성이 있다.

## 면접식 답변

> TCP는 연결 지향적이고 신뢰성 있는 전송을 제공하는 L4 프로토콜입니다. 연결은 SYN, SYN-ACK, ACK의 3-way handshake로 수립되고, 이후 sequence number와 ACK를 이용해 순서와 재전송을 관리합니다.

---

# 9. UDP

UDP는 TCP보다 단순한 L4 프로토콜이다. RFC 768은 UDP가 packet-switched network에서 datagram mode 통신을 제공하기 위한 프로토콜이라고 설명한다.

## 특징

```
연결 없음재전송 없음순서 보장 없음헤더가 작음지연이 적음
```

## 어디에 쓰나

```
DNSDHCPVoIP게임실시간 스트리밍 일부
```

## TCP와 비교

|구분|TCP|UDP|
|---|---|---|
|연결|있음|없음|
|신뢰성|높음|낮음|
|순서 보장|있음|없음|
|속도/지연|상대적으로 무거움|가벼움|
|예시|HTTP, SSH|DNS, 실시간 전송|

## 면접식 답변

> UDP는 연결 수립 없이 datagram을 보내는 전송 프로토콜입니다. TCP처럼 신뢰성이나 순서 보장은 없지만 구조가 단순하고 지연이 낮아 DNS나 실시간 통신에 자주 사용됩니다.

---

# 10. L4 Load Balancing

L4 로드밸런싱은 IP와 Port, TCP/UDP 연결 정보를 기준으로 트래픽을 여러 서버에 나누는 것이다.

## 예시

```
Client → VIP:443Load Balancer → Backend1:443              → Backend2:443
```

클라이언트는 VIP 하나만 본다. 로드밸런서가 내부 서버로 나눠준다.

## 기준 정보

L4에서는 보통 이런 정보를 본다.

```
Source IPDestination IPSource PortDestination PortProtocol: TCP/UDP
```

## 5-Tuple

네트워크 세션을 구분할 때 자주 쓰는 묶음이다.

```
Source IPDestination IPSource PortDestination PortProtocol
```

## 세션 테이블

로드밸런서는 어떤 클라이언트 연결을 어느 backend로 보냈는지 기억해야 한다.

예:

```
client 1.1.1.1:50000 → backend 10.0.0.2:443
```

그래야 이후 같은 연결의 패킷이 같은 backend로 간다.

## 알고리즘

대표적으로:

```
Round RobinLeast ConnectionSource IP HashWeighted Round Robin
```

## 면접식 답변

> L4 로드밸런싱은 IP, Port, Protocol 같은 L4 정보를 기준으로 클라이언트 연결을 여러 backend 서버에 분산하는 방식입니다. TCP 연결 단위의 일관성을 유지하기 위해 세션 테이블 관리가 중요합니다.

---

# 11. HTTP / L7

HTTP는 L7 응용 계층 프로토콜이다. RFC 9110은 HTTP를 distributed, collaborative, hypertext information system을 위한 stateless application-level protocol로 설명한다.

## HTTP Request 구조

예:

```
GET /api/users HTTP/1.1Host: example.comUser-Agent: curl/8.0Accept: */*
```

핵심:

```
Method: GET, POST, PUT, DELETEPath: /api/usersVersion: HTTP/1.1Header: Host, User-Agent 등Body: POST 데이터 등
```

## HTTP Response 구조

```
HTTP/1.1 200 OKContent-Type: application/jsonContent-Length: 15{"result":"ok"}
```

## Stateless

HTTP는 기본적으로 요청 하나하나가 독립적이다. 서버가 상태를 기억하지 않는다는 설계다. 그래서 로그인 상태 유지에는 Cookie, Session, Token 같은 추가 구조가 필요하다.

## Keep-Alive

TCP 연결을 요청마다 새로 만들지 않고 재사용하는 방식이다.

장점:

```
TCP handshake 비용 감소성능 개선
```

주의점:

```
connection 관리 필요idle timeout 필요동시 연결 수 증가 가능
```

## L7 Load Balancing

L7은 HTTP 내용을 보고 분기할 수 있다.

예:

```
/api/*      → backend-api/image/*    → backend-imageHost: a.com → backend-aHost: b.com → backend-b
```

L4보다 더 똑똑하지만, HTTP를 파싱해야 해서 처리 비용은 더 크다.

## 면접식 답변

> L7 로드밸런싱은 TCP/IP 정보뿐 아니라 HTTP method, path, host, header 같은 응용 계층 정보를 보고 backend를 선택하는 방식입니다. L4보다 정교한 분기가 가능하지만 HTTP 파싱과 연결 관리 비용이 발생합니다.

---

# 12. SSL/TLS, SSL Offloading

현재 정확한 용어는 TLS가 맞고, SSL은 오래된 표현이지만 업계에서는 “SSL 장비”, “SSL offloading”처럼 관성적으로 많이 쓴다. RFC 8446은 TLS 1.3이 client/server application이 인터넷에서 도청, 변조, 위조를 방지하도록 통신하게 해주는 프로토콜이라고 설명한다.

## TLS가 하는 일

```
암호화무결성 검증서버 인증선택적으로 클라이언트 인증
```

## HTTPS

HTTP + TLS다.

```
HTTP: 평문HTTPS: TLS로 보호되는 HTTP
```

## SSL/TLS Handshake

대략 하는 일:

```
암호화 방식 협상인증서 확인키 교환세션 키 생성
```

## SSL Offloading

서버 대신 로드밸런서/ADC 장비가 TLS 복호화를 처리하는 방식이다.

예:

```
Client --HTTPS--> L4/L7 장비 --HTTP 또는 HTTPS--> Backend
```

장점:

```
Backend 서버의 TLS 처리 부담 감소중앙 인증서 관리L7 HTTP 내용 분석 가능
```

주의점:

```
복호화 지점의 보안 중요내부망 평문 전송 여부 정책 필요인증서 관리 필요
```

## 면접식 답변

> SSL offloading은 클라이언트와의 TLS 처리를 로드밸런서나 ADC 장비가 대신 수행하는 구조입니다. 이를 통해 backend 서버의 암호화 처리 부담을 줄이고, 복호화된 HTTP 정보를 기준으로 L7 정책을 적용할 수 있습니다.

---

# 13. Linux Socket Programming

Linux socket은 사용자 프로세스가 네트워크 프로토콜 스택과 통신하기 위한 인터페이스다. Linux manual은 BSD 호환 socket이 user process와 kernel network protocol stack 사이의 uniform interface라고 설명한다.

## 서버 기본 흐름

C 서버는 보통 이렇게 간다.

```
socket()bind()listen()accept()recv()send()close()
```

## socket()

통신용 파일 디스크립터를 만든다.

```
int fd = socket(AF_INET, SOCK_STREAM, 0);
```

의미:

```
AF_INET: IPv4SOCK_STREAM: TCP
```

## bind()

소켓에 IP와 Port를 붙인다. Linux manual은 SOCK_STREAM socket이 connection을 받으려면 보통 bind로 local address를 할당해야 한다고 설명한다.

```
bind(fd, ...);
```

## listen()

서버가 연결 요청을 받을 준비를 한다.

```
listen(fd, backlog);
```

## accept()

클라이언트 연결을 수락하고, 실제 통신용 새 socket fd를 반환한다. Linux manual은 accept가 pending connection queue에서 첫 요청을 꺼내 새 connected socket을 만든다고 설명한다.

```
int client_fd = accept(server_fd, ...);
```

## recv() / send()

데이터를 받고 보낸다.

```
recv(client_fd, buffer, sizeof(buffer), 0);send(client_fd, buffer, len, 0);
```

## close()

연결을 닫는다.

```
close(client_fd);
```

## 클라이언트 기본 흐름

```
socket()connect()send()recv()close()
```

## 면접식 답변

> Linux socket programming은 애플리케이션이 커널의 네트워크 스택을 사용하기 위한 인터페이스입니다. TCP 서버는 socket, bind, listen, accept 이후 recv/send로 데이터를 주고받습니다.

---

# 14. Blocking / Non-blocking I/O

## Blocking

데이터가 올 때까지 함수가 멈춘다.

```
recv(fd, buf, size, 0);
```

데이터가 안 오면 여기서 대기한다.

장점:

```
코드가 단순함
```

단점:

```
동시 접속 처리에 불리함
```

## Non-blocking

데이터가 없으면 바로 반환한다.

```
읽을 데이터 없음 → EAGAIN / EWOULDBLOCK
```

장점:

```
많은 연결을 한 스레드에서 처리 가능
```

단점:

```
상태 관리가 복잡함
```

## 왜 중요하냐

네트워크 장비나 서버는 동시에 많은 연결을 처리해야 한다. blocking 방식으로 연결마다 스레드를 만들면 규모가 커질수록 부담이 커진다.

---

# 15. select / poll / epoll

여러 socket을 동시에 감시하는 I/O multiplexing 기술이다.

## select

오래된 방식이다.

특징:

```
fd set 크기 제한매번 fd 집합을 넘김대량 연결에 불리
```

## poll

select보다 fd 제한 면에서 낫지만, 감시 대상이 많으면 매번 훑어야 한다.

## epoll

Linux에서 대규모 fd 감시에 많이 쓰는 방식이다. epoll manual은 epoll이 여러 file descriptor의 I/O 가능 여부를 감시하는 기능이며, edge-triggered와 level-triggered 방식으로 사용할 수 있고 많은 fd에 대해 확장성이 좋다고 설명한다.

기본 흐름:

```
epoll_create1()epoll_ctl()epoll_wait()
```

## Level-triggered

읽을 데이터가 남아 있으면 계속 이벤트가 온다.

## Edge-triggered

상태가 변하는 순간에만 이벤트가 온다. 성능은 좋을 수 있지만, 데이터를 끝까지 읽지 않으면 이벤트를 놓칠 수 있다.

## 면접식 답변

> select, poll, epoll은 여러 socket의 I/O 이벤트를 감시하는 방식입니다. epoll은 Linux에서 대량의 파일 디스크립터를 효율적으로 감시하기 위해 많이 사용되며, 네트워크 서버나 장비 관리 데몬 구현에서 중요합니다.

---

# 16. SO_REUSEADDR, TCP_NODELAY

## SO_REUSEADDR

서버 재시작 시 같은 포트에 bind가 안 되는 문제를 줄이는 데 자주 쓴다. Linux socket manual은 SO_REUSEADDR이 bind에서 local address 재사용 규칙을 완화한다고 설명한다.

```
setsockopt(fd, SOL_SOCKET, SO_REUSEADDR, ...);
```

## TCP_NODELAY

Nagle 알고리즘을 끄는 옵션이다. 작은 패킷을 즉시 보내야 하는 경우 지연을 줄일 수 있다.

주의점:

```
지연은 줄지만 작은 패킷이 많아져 효율이 떨어질 수 있음
```

---

# 17. Byte Order / Endianness

네트워크 프로토콜에서는 byte order가 중요하다.

## Big Endian / Little Endian

- Big endian: 큰 바이트가 앞
- Little endian: 작은 바이트가 앞

네트워크 byte order는 big endian이다.

## 함수

```
htons()htonl()ntohs()ntohl()
```

의미:

```
h: hostn: networks: shortl: long
```

예:

```
server_addr.sin_port = htons(8080);
```

이걸 빼먹으면 port나 IP 값이 이상하게 해석된다.

---

# 18. Packet Parsing

Packet parsing은 raw packet에서 header를 해석하는 것이다.

예를 들어 Ethernet frame을 받으면:

```
Ethernet Header→ IPv4 Header→ TCP Header→ Payload
```

이 순서로 해석한다.

## Ethernet Header

```
struct ethhdr {    unsigned char dst[6];    unsigned char src[6];    unsigned short ethertype;};
```

## IPv4 Header

중요 필드:

```
versionihltotal lengthttlprotocolsource IPdestination IP
```

## TCP Header

중요 필드:

```
source portdestination portsequence numberack numberflagswindow size
```

## 주의점

패킷 파서는 보안상 위험한 코드가 되기 쉽다.

반드시 확인해야 하는 것:

```
패킷 길이가 header보다 짧지 않은가IHL 값이 정상인가total length가 실제 capture 길이보다 크지 않은가TCP header length가 정상인가
```

## 면접식 답변

> 패킷 파싱은 Ethernet, IP, TCP/UDP header를 순서대로 해석해 출발지, 목적지, 프로토콜, 포트, payload 위치를 알아내는 작업입니다. 외부 입력을 다루기 때문에 길이 검증과 경계값 처리가 중요합니다.

---

# 19. tcpdump / Wireshark

## tcpdump

tcpdump는 네트워크 인터페이스에서 조건에 맞는 패킷 내용을 출력하거나 파일로 저장할 수 있는 도구다. tcpdump manual은 interface에서 boolean expression에 맞는 packet 내용을 출력하고, `-w`로 저장하거나 `-r`로 저장된 파일을 읽을 수 있다고 설명한다.

예:

```
sudo tcpdump -i eth0sudo tcpdump -i eth0 arpsudo tcpdump -i eth0 tcp port 80sudo tcpdump -i eth0 -w capture.pcap
```

## Wireshark

GUI 기반 패킷 분석 도구다. tcpdump로 `.pcap` 파일을 저장하고 Wireshark로 열어 분석할 수 있다.

## 봐야 하는 것

```
ARP request/replyICMP echo request/replyTCP SYN/SYN-ACK/ACKHTTP GET/POSTTLS handshakeDNS query/response
```

## 면접식 답변

> tcpdump와 Wireshark는 실제 네트워크에서 어떤 패킷이 오가는지 확인하는 도구입니다. 로그만으로 원인을 알기 어려울 때 패킷 캡처를 통해 ARP, TCP handshake, HTTP 요청, DNS 응답 등을 직접 확인할 수 있습니다.

---

# 20. iproute2: ip addr, ip link, ip route

iproute2는 Linux 네트워크 설정을 다루는 핵심 도구 묶음이다. `ip route` manual은 이 명령이 kernel routing table의 route entry를 조작하는 도구라고 설명한다.

## ip addr

IP 주소 확인/설정.

```
ip addr
```

## ip link

인터페이스 상태 확인/설정.

```
ip linkip link set eth0 upip link set eth0 down
```

## ip route

라우팅 테이블 확인/설정.

```
ip route
```

## ip neigh

ARP cache 확인.

```
ip neigh
```

## 면접식 답변

> Linux에서는 iproute2 명령으로 인터페이스 상태, IP 주소, 라우팅 테이블, ARP cache를 확인할 수 있습니다. 네트워크 장애 분석에서 기본적으로 확인해야 하는 도구입니다.

---

# 21. Linux Bridge / Network Namespace

## Linux Bridge

Linux에서 소프트웨어 스위치처럼 동작하는 기능이다. 여러 interface를 묶어서 L2 forwarding을 수행한다. kernel bridge 문서는 bridge가 여러 network segment를 연결하고 destination MAC 주소 기준으로 frame을 forward한다고 설명한다.

## Network Namespace

하나의 Linux 안에 여러 개의 독립된 네트워크 공간을 만드는 기능이다.

예:

```
ns1: 독립된 PC처럼 동작ns2: 독립된 PC처럼 동작bridge: 가상 스위치
```

이걸 이용하면 실제 스위치 없이도 L2/L3 실습이 가능하다.

## 실습 구조

```
ns1 --- veth --- bridge --- veth --- ns2
```

## 할 수 있는 실습

```
ARP 확인ICMP ping 확인bridge fdb 확인VLAN 설정라우팅 실습iptables 차단
```

---

# 22. iptables / nftables

iptables와 nftables는 Linux packet filtering, NAT, firewall rule을 다루는 도구다. Netfilter 공식 문서는 iptables, nftables, connection tracking 등 관련 문서를 제공하고, nftables가 기존 iptables 계열을 대체하는 packet classification framework라고 설명한다.

## packet filtering

조건에 맞는 패킷을 허용하거나 차단한다.

예:

```
sudo iptables -A INPUT -p tcp --dport 22 -j ACCEPTsudo iptables -A INPUT -p tcp --dport 80 -j DROP
```

## NAT

주소를 바꾸는 기능이다.

대표적으로:

```
SNATDNATMASQUERADE
```

## Connection Tracking

연결 상태를 추적한다.

예:

```
NEWESTABLISHEDRELATEDINVALID
```

## 면접식 답변

> iptables와 nftables는 Linux에서 패킷 필터링, NAT, connection tracking을 설정하는 도구입니다. 보안스위치나 방화벽의 정책 처리 개념을 실습할 때 유용합니다.

---

# 23. NAT

NAT는 Network Address Translation이다. IP 주소 또는 port를 변환한다.

## SNAT

출발지 주소를 바꾼다.

예:

```
내부 PC 192.168.0.10 → 인터넷출발지 IP를 공유기 공인 IP로 변경
```

## DNAT

목적지 주소를 바꾼다.

예:

```
공인 IP:80 → 내부 서버 192.168.0.100:80
```

## PAT / NAPT

IP뿐 아니라 port까지 바꿔서 여러 내부 PC가 하나의 공인 IP를 공유한다.

## 면접식 답변

> NAT는 패킷의 IP 주소나 port를 변환하는 기능입니다. SNAT은 출발지 주소를 바꾸고, DNAT은 목적지 주소를 바꿉니다. 일반 가정 공유기는 보통 NAPT를 이용해 여러 내부 단말이 하나의 공인 IP를 공유하게 합니다.

---

# 24. ARP Spoofing

ARP spoofing은 공격자가 거짓 ARP 정보를 보내서 피해자의 트래픽을 자신에게 오게 만드는 L2 공격이다. OWASP는 ARP spoofing을 공격자가 gateway로 가장해 트래픽을 공격자 장비로 보내게 만드는 Layer 2 공격으로 설명한다.

## 예시

정상 상황:

```
Gateway IP 192.168.0.1 → Gateway MAC AA:AA
```

공격자가 거짓 ARP를 보냄:

```
192.168.0.1 is at 공격자 MAC BB:BB
```

피해자는 gateway로 가야 할 트래픽을 공격자에게 보낸다.

## 대응

```
Static ARPDynamic ARP InspectionARP table 이상 탐지MAC-IP binding스위치 보안 기능
```

## 면접식 답변

> ARP spoofing은 거짓 ARP 응답으로 IP-MAC 매핑을 속여 트래픽을 공격자 쪽으로 우회시키는 L2 공격입니다. 보안스위치에서는 비정상 ARP 변화 탐지나 MAC-IP binding 같은 방식으로 대응할 수 있습니다.

---

# 25. Port Scanning

Port scanning은 대상 장비의 어떤 port가 열려 있는지 확인하는 행위다.

예:

```
22/tcp open ssh80/tcp open http443/tcp open https
```

## 왜 보안에서 중요한가

열린 port는 공격 표면이다. 불필요한 서비스가 열려 있으면 취약점 노출 가능성이 커진다.

## 대표 방식

```
TCP connect scanSYN scanUDP scan
```

## 방어 관점

```
불필요 port 차단ACL 적용scan 빈도 탐지로그 기록
```

---

# 26. DoS / DDoS

## DoS

Denial of Service. 특정 서버나 네트워크 장비의 자원을 고갈시켜 정상 서비스를 방해하는 공격이다.

## DDoS

Distributed DoS. 여러 공격 지점에서 동시에 트래픽을 보내는 방식이다.

## 예시

```
SYN floodUDP floodICMP floodHTTP request flood
```

## 네트워크 장비 관점

장비가 해야 할 것:

```
비정상 트래픽 탐지rate limitingconnection limitblacklist정상 트래픽 보호로그 기록
```

---

# 27. ACL

ACL은 Access Control List다. 조건에 따라 트래픽을 허용하거나 차단하는 규칙이다.

예:

```
10.0.0.0/24에서 오는 SSH 허용그 외 SSH 차단특정 MAC 주소 차단특정 VLAN 간 통신 차단
```

## 기준

```
Source IPDestination IPProtocolSource PortDestination PortMAC AddressVLAN
```

## 면접식 답변

> ACL은 IP, port, protocol, MAC, VLAN 같은 조건을 기준으로 트래픽을 허용하거나 차단하는 정책입니다. 보안스위치나 방화벽에서 기본적인 접근 제어 기능으로 사용됩니다.

---

# 28. 802.1X

802.1X는 네트워크 접속 인증을 위한 방식이다.

## 구성요소

```
Supplicant: 접속하려는 단말Authenticator: 스위치/APAuthentication Server: 보통 RADIUS 서버
```

## 목적

인증되지 않은 단말이 네트워크에 접속하지 못하게 한다.

## 흐름

```
단말 연결→ 스위치가 인증 요구→ RADIUS 서버로 인증→ 성공하면 포트 허용→ 실패하면 차단 또는 격리 VLAN
```

## 면접식 답변

> 802.1X는 단말이 네트워크에 접속하기 전에 인증을 수행하는 포트 기반 접근 제어 기술입니다. 스위치가 authenticator 역할을 하고, RADIUS 서버를 통해 인증 결과를 받아 포트를 허용하거나 차단합니다.

---

# 29. C/C++ 코드 품질: 네트워크 장비 개발에서 중요한 것

네트워크 장비 개발은 외부 입력을 계속 받는다. 그래서 C/C++에서 실수하면 바로 장애나 보안 취약점이 된다.

## 포인터

주소를 직접 다루는 변수다.

위험한 경우:

```
char *p = NULL;*p = 'a';   // crash
```

## Buffer Overflow

버퍼 크기보다 더 많이 쓰는 문제다.

```
char buf[8];strcpy(buf, "this is too long");
```

결과:

```
메모리 손상crash보안 취약점
```

## Memory Leak

할당한 메모리를 해제하지 않는 문제다.

```
char *p = malloc(100);// free(p) 없음
```

장비처럼 오래 켜져 있는 프로그램에서는 치명적이다.

## Use-After-Free

free한 메모리를 다시 사용하는 문제다.

```
free(p);*p = 1;
```

AddressSanitizer는 C/C++에서 heap/stack/global out-of-bounds, use-after-free 같은 메모리 오류를 탐지할 수 있는 도구다.

## Race Condition

멀티스레드에서 공유 자원을 동시에 건드려 생기는 문제다.

예:

```
thread A: connection count 증가thread B: connection count 감소동시에 접근해서 값 꼬임
```

## Error Code 설계

장비 개발에서는 실패 원인을 명확히 남겨야 한다.

나쁜 예:

```
failed
```

좋은 예:

```
ERR_SOCKET_BIND_FAILEDERR_CONFIG_PARSE_FAILEDERR_BACKEND_CONNECT_TIMEOUT
```

## Log Level

```
DEBUG: 개발 중 상세 정보INFO: 정상 흐름WARN: 이상하지만 계속 동작 가능ERROR: 기능 실패FATAL: 프로그램 중단 수준
```

## 면접식 답변

> 네트워크 장비 개발에서는 외부 패킷과 설정 입력을 계속 처리하므로, C/C++ 코드에서 buffer overflow, memory leak, use-after-free, race condition을 방지하는 것이 중요합니다. 또한 장애 분석을 위해 에러 코드와 로그 레벨을 명확히 설계해야 합니다.

---

# 30. CMake / Makefile

## Makefile

빌드 명령을 직접 정의하는 파일이다.

예:

```
main: main.c	gcc main.c -o main
```

## CMake

빌드 시스템을 생성해주는 도구다. CMake 공식 사이트는 CMake를 소프트웨어 빌드 프로세스를 관리하기 위한 도구로 설명한다.

예:

```
cmake_minimum_required(VERSION 3.16)project(tcp_proxy C)add_executable(tcp_proxy main.c)
```

## 왜 중요한가

회사 코드는 파일 하나로 끝나지 않는다.

```
src/include/tests/lib/CMakeLists.txt
```

이런 구조를 다룰 줄 알아야 한다.

---

# 31. 성능 측정 개념

네가 “성능과 안정성”을 말하려면 숫자로 설명할 줄 알아야 한다.

## Throughput

단위 시간당 처리량이다.

```
MbpsGbpsrequests/secpackets/sec
```

## Latency

요청이 들어와 응답이 나가기까지 걸린 시간이다.

```
평균 latency최대 latencyp95 latencyp99 latency
```

## Packet Loss

보낸 패킷 중 손실된 비율이다.

```
loss = lost packets / sent packets * 100
```

예:

```
1000개 전송10개 손실loss = 10 / 1000 * 100 = 1%
```

## Jitter

지연시간의 변동폭이다. 실시간 통신에서 중요하다.

## CPU / Memory Usage

성능 저하가 코드 때문인지, CPU 병목인지, 메모리 부족인지 봐야 한다.

## Connection Count

동시 연결 수다.

```
현재 연결 1000개초당 신규 연결 500개
```

## iperf3

iperf3는 IP 네트워크에서 최대 달성 가능한 bandwidth를 능동적으로 측정하는 도구이고, 각 테스트에서 throughput, loss 등 값을 보고한다.

## 면접식 답변

> 성능은 단순히 빠르다는 말이 아니라 throughput, latency, packet loss, CPU 사용률, memory 사용량, 동시 연결 수 같은 지표로 확인해야 합니다. 네트워크 장비 개발에서는 기능 구현 후 부하 조건에서 수치로 검증하는 과정이 중요합니다.

---

# 32. 안정성 개념

안정성은 “평소에 잘 된다”가 아니다. 장애 상황에서도 예측 가능하게 동작해야 한다.

## 봐야 하는 것

```
장시간 실행 시 memory leak 없는가backend 장애 시 복구되는가연결 폭주 시 죽지 않는가설정 오류 시 안전하게 실패하는가로그가 남는가재시작 후 정상 복구되는가
```

## Graceful Degradation

문제가 생겨도 전체가 죽지 않고 일부 기능만 제한되는 것.

예:

```
backend 3개 중 1개 장애→ 나머지 2개로 트래픽 우회
```

## Timeout

네트워크 코드는 timeout이 중요하다.

```
connect timeoutread timeoutidle timeouthealth check timeout
```

timeout이 없으면 한 연결에 무한히 매달릴 수 있다.

---

# 33. Health Check

로드밸런서가 backend 서버가 살아 있는지 확인하는 기능이다.

## 방식

```
TCP connect checkHTTP GET /healthICMP pingCustom protocol check
```

## 상태

```
UPDOWNDEGRADED
```

## 왜 중요한가

backend가 죽었는데 계속 트래픽을 보내면 장애가 커진다.

## 면접식 답변

> Health check는 backend 서버의 상태를 주기적으로 확인해 정상 서버에만 트래픽을 보내기 위한 기능입니다. L4에서는 TCP 연결 확인, L7에서는 HTTP health endpoint 확인 방식이 사용될 수 있습니다.

---

# 34. Log / 관리 구조

네 강점과 가장 잘 맞는 부분이다.

## 좋은 로그

좋은 로그는 나중에 원인을 추적할 수 있어야 한다.

예:

```
timestamplevelmoduleclient_ipbackend_ipsession_iderror_codelatencybytes_in/out
```

## 나쁜 로그

```
error occurredfailednot working
```

원인 추적이 안 된다.

## 장비 관리 기능

네트워크 장비에는 보통 이런 관리 기능이 필요하다.

```
CLIGUISNMPREST APISyslogNTP설정 백업/복구상태 조회펌웨어 업데이트
```

## 면접식 답변

> 장비 개발에서 로그와 관리 구조는 장애 대응 속도와 직접 연결됩니다. 단순히 오류를 출력하는 것이 아니라, 언제, 어떤 모듈에서, 어떤 입력과 상태로 문제가 발생했는지 추적 가능한 구조가 필요합니다.

---

# 35. CLI

CLI는 Command Line Interface다.

네 STM32 UART 프로젝트와 가장 직접 연결된다.

## 예시

```
show interfaceshow versionshow logset vlan 10 port 1restart service
```

## CLI가 중요한 이유

```
현장 엔지니어가 빠르게 상태 확인 가능GUI가 죽어도 접근 가능자동화 스크립트와 연동 가능장애 대응에 유리
```

## 면접식 답변

> CLI는 장비의 상태 조회와 설정 변경을 위한 기본 관리 인터페이스입니다. 현장 장애 대응에서는 GUI보다 빠르고 자동화하기 쉬워 네트워크 장비에서 중요합니다.

---

# 36. GUI

GUI는 그래픽 기반 관리 화면이다.

## 기능

```
장비 상태 대시보드트래픽 그래프포트 상태 표시장애 알림설정 변경로그 검색
```

## 개발자가 알아야 하는 점

GUI 자체를 프론트엔드만으로 보면 안 된다. 장비 내부 API, 상태 수집 daemon, 로그 시스템과 연결된다.

---

# 37. NTP

NTP는 Network Time Protocol이다. 장비 시간을 맞추는 데 사용한다.

## 왜 중요한가

로그 시간이 맞지 않으면 장애 분석이 어려워진다.

예:

```
스위치 로그: 10:01:03방화벽 로그: 09:58:44서버 로그: 10:04:12
```

시간이 어긋나면 사건 순서를 맞추기 어렵다.

## 면접식 답변

> NTP는 장비 시간을 동기화하기 위한 프로토콜입니다. 여러 장비의 로그를 함께 분석해야 하는 네트워크 환경에서는 시간 동기화가 장애 분석의 기본입니다.

---

# 38. 테스트 케이스

테스트 케이스는 기능이 의도대로 동작하는지 확인하는 절차다.

## 예시

```
테스트명: VLAN 분리 확인조건: port1 VLAN10, port2 VLAN20절차: port1 PC에서 port2 PC로 ping기대결과: 통신 실패
```

## 좋은 테스트 케이스 구성

```
목적환경입력절차기대결과실제결과판정로그/증거
```

## 회귀 테스트

수정 후 기존 기능이 깨지지 않았는지 다시 확인하는 테스트다.

## 면접식 답변

> 테스트 케이스는 기능 검증을 재현 가능한 절차로 정리한 것입니다. 장비 개발에서는 기능 추가 후 기존 기능이 깨지지 않았는지 확인하는 회귀 테스트도 중요합니다.

---

# 39. 장애 재현 문서

장애를 고치려면 먼저 재현해야 한다.

## 구성

```
증상환경버전설정값재현 절차기대 동작실제 동작로그패킷 캡처임시 조치근본 원인수정 내용
```

## 네가 자기소개서에서 강하게 가져갈 부분

너는 “로그 기반 디버깅” 경험을 계속 말하고 있으니, 이걸 문서화와 연결하면 좋다.

> 저는 문제를 감으로 처리하지 않고, 입력 조건과 로그, 재현 절차를 기준으로 원인을 좁히는 방식에 익숙합니다.

이 문장은 괜찮다. 단, 실제 GitHub에 `troubleshooting.md`가 있으면 훨씬 강해진다.

---

# 40. 지금 네가 우선 공부해야 할 순서

다 한 번에 파면 망한다. 순서가 중요하다.

## 1단계: L2/L3 기본

```
EthernetMACSwitchingARPIPSubnetGatewayRoutingICMPVLANSTP
```

## 2단계: 패킷 분석

```
tcpdumpWiresharkARP 캡처ICMP 캡처TCP handshake 캡처HTTP 요청 캡처TLS handshake 캡처
```

## 3단계: Linux socket

```
TCP echo serverTCP clientselect serverepoll server
```

## 4단계: L4/L7 미니 구현

```
TCP proxyRound-robin load balancerHTTP reverse proxyHealth checkAccess log
```

## 5단계: 보안스위치 기능 모사

```
ARP spoofing 탐지비인가 MAC 차단iptables/nftables ACLport scan 로그 분석DoS성 트래픽 rate limit 개념
```

---

# 41. 네 자기소개서 문장을 역량으로 바꾸면

현재 문장:

> L2/L3 스위칭과 L4/L7 패킷 처리 구조를 깊이 이해하고

이걸 실제 역량으로 만들려면 아래를 해야 한다.

```
Linux network namespace로 L2/L3 실습tcpdump/Wireshark로 ARP, ICMP, TCP handshake 분석C socket으로 TCP echo server 구현epoll 기반 다중 접속 서버 구현TCP proxy와 HTTP reverse proxy 구현iperf3로 처리량과 지연시간 측정
```

현재 문장:

> 성능과 안정성을 함께 고려하는 개발자

이걸 실제 역량으로 만들려면 아래를 해야 한다.

```
처리량 측정latency 측정packet loss 측정CPU/memory 사용률 확인장시간 실행 테스트connection 폭주 테스트timeout 처리health check장애 로그 설계
```

현재 문장:

> 장애 상황에서 원인을 파악할 수 있는 로그와 관리 구조

이건 네가 이미 제일 잘 연결할 수 있는 부분이다.

```
STM32 UART CLIDobot 데이터 수집 로그ROS2 topic/service 디버깅SignGlove 센서 데이터 수집
```

여기에 네트워크 쪽 실습만 붙이면 설득력이 생긴다.

---

# 42. 냉정한 결론

지금 네가 부족한 건 “머리가 나쁘다”가 아니라 **경험의 방향이 네트워크 장비로 아직 정렬되지 않았다는 것**이다.

네 기존 경험은 이쪽이다.

```
장비 제어센서 데이터 수집UART 통신ROS2 시스템 통합카메라/로봇 데이터 파이프라인로그 기반 디버깅
```

파이오링크 직무가 요구하는 방향은 이쪽이다.

```
Ethernet frameMAC learningVLAN/STPIP routingTCP/UDP sessionL4 load balancingHTTP parsingTLS offloading패킷 분석성능 측정보안 트래픽 탐지/차단
```

두 영역의 연결고리는 있다.

```
통신장비 상태 관리로그디버깅LinuxC/C++실제 장비 안정성
```

하지만 연결고리만으로는 약하다. 그래서 최소한 아래 4개는 포트폴리오로 만들어야 한다.

```
1. Linux namespace + bridge + VLAN 실습2. tcpdump/Wireshark 패킷 분석 README3. C 기반 TCP echo/epoll server4. 간단한 TCP proxy 또는 HTTP reverse proxy
```